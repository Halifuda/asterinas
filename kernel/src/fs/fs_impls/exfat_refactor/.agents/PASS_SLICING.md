<!-- SPDX-License-Identifier: MPL-2.0 -->

# Main-Agent Pass Slicing Ledger

This file is the durable main-agent-owned record of how meso-level Architect / Designer contracts are split into pass-level Creator, Checker, and Reviewer work.

`SYSTEM_BLUEPRINT.md` remains the active status board. This ledger records the scheduling decision, covered-micro boundary, and rationale so later main agents do not rediscover or accidentally widen previous pass slices.

## Rules

- Only the main agent updates this file.
- Record a decision before or at the same time a Creator, Checker, or Reviewer packet is dispatched.
- Keep Designer artifacts meso-scoped; do not ask Designers to pre-slice implementation passes.
- Every Creator-synced Checker pass mirrors its Creator pass exactly.
- Keep meso integration passes separate from Creator-synced Checker passes.
- When a structural cleanup pass exists, list each cleanup objective separately and record whether it is fully closed or intentionally deferred.

## Current Pass Slicing Decisions

### `xfstests_linux_baseline_20260624`

**Decision:** The June 24 Linux-loop exFAT baseline under `.agents/tmp/` is the current broad upstream-suite scheduling baseline for `exfat_refactor`. The unfiltered receipt `test-exfat-linux.txt` ran the generic candidate space and records 633 default `[not run]` exclusions, mostly exFAT capability exclusions such as symlink, hardlink, xattr, ACL, reflink, dedupe, fallocate, fiemap, quota, and journaling features. The filtered receipt `test-exfat-linux-run.txt` is the actionable baseline: 151 tests actually ran on Linux exfat, with 147 passes and 4 Linux-side failures (`generic/169`, `generic/528`, `generic/563`, `generic/740`). Main-agent slicing keeps the runnable Linux-exfat set in three named runlists and does not schedule broad `short.list`, `full.list`, multi-section, or overlay execution yet.

| Pass ID | Kind | Covered Boundary | Slicing Rationale | Decision State |
| :--- | :--- | :--- | :--- | :--- |
| `pass_01_linux_baseline_simple_checker` | Meso Integration Pass | Run only `test/initramfs/src/conformance/xfstests/exfat_refactor_pass1_simple.list`: `generic/001`, `generic/006`, `generic/007`, `generic/011`, `generic/013`, `generic/028`, `generic/035`, `generic/100`, `generic/245`, `generic/257`, `generic/309`, `generic/313`, `generic/471`, `generic/637`, `generic/676`, `generic/763`. | Small first slice biased toward already proven smoke plus ordinary directory, rename, truncate, readdir, and basic stress behavior. This pass is allowed to use `SCRATCH_DEV`, but it should still be treated as the low-blast-radius baseline before reopening the larger Linux-run set. | Runlist created; not dispatched or executed in this session. |
| `pass_02_linux_baseline_difficult_checker` | Meso Integration Pass | Run only `test/initramfs/src/conformance/xfstests/exfat_refactor_pass2_difficult.list`: the 73 Linux-exfat-passing tests left after pass 01 / pass 03 extraction and after removing current known failure `generic/003`. | These tests passed on Linux exfat but are materially more likely to expose Asterinas filesystem or VFS issues: scratch lifecycle, mmap, fsx/fsstress, remount, copy-range, seek, timestamp, mount, page-cache, and larger-file paths. Keep them separate so pass 01 failures do not pollute the harder batch. `generic/003` is recorded as a current known failure because it leaves SCRATCH mounted/read-only and pollutes the rest of the batch. | Runlist rerun without `generic/003`; broad result archived at `.agents/tmp/20260624-pass2-difficult-without-003-result.txt`. The useful through-`generic/452` boundary is preserved at `.agents/tmp/20260624-pass2-through-452-preserved-result.txt`; the fresh suffix rerun from `generic/460` is archived at `.agents/tmp/20260624-pass2-after-452-result.txt`. |
| `pass_03_linux_baseline_likely_fail_checker` | Meso Integration Pass | Run only `test/initramfs/src/conformance/xfstests/exfat_refactor_pass3_likely_fail.list`: the 4 Linux-exfat failures plus Linux-passing tests dominated by Asterinas OS substrate risk such as direct I/O, AIO, io_uring, dm/flakey/thin, shutdown, swap, cgroup, scsi_debug, idmapped/capability, THP, CPU hotplug, and destructive block I/O. | This is intentionally last and may require separate non-exFAT repair waves. Some entries are also affected by the current upstream `block.list`; do not remove those exclusions merely to force this pass to run. | Runlist created; not dispatched or executed in this session. |
| `pass_04_regular_file_page_cache_context_merge_creator` | Creator-Synced Pass | Historical rejected route only: this pass attempted to replace temporary `install_page_cache_context(...)` with one inode-state-owned regular-file page-cache carrier reached through `inode_state`, while keeping directory / metadata paths out of scope. This row is retained only so later main agents can identify the archived dispatch/artifact pair that must not be resumed. | Main-agent review proved the route was structurally wrong, not merely unfinished. Outer mutation and sync paths still needed to hold `inode_state.write()` and `mount_state.write()` across backend-capable page-cache operations, while backend callbacks still needed current file mapping and mount runtime facts. That creates self-reentry pressure on the same state the outer call already holds. | Rejected after review; experimental Rust edits were reverted. Archived dispatches/artifact remain historical only and must not be continued. |
| `pass_05_regular_file_page_cache_owner_split_creator` | Creator-Synced Pass | Route the June 24 `generic/313` / `generic/639` regular-file first-write growth failure into a clean owner split under `meso_06_file_content_mutation`: `ExfatFs` owns backend-readable mount runtime state, `ExfatInode` keeps persistent regular-file mapping state via the existing `PageCacheContext`, and `ExfatFilePageBackend` keeps only `Weak<ExfatInode>` while reading current owner state under short lock holds. Remove temporary `install_page_cache_context(...)` / `PageCacheContextGuard`; keep directory stream and directory lock families out of scope; do not introduce a callback-lifetime snapshot carrier. | The rejected pass showed that “merge it under `inode_state`” is the wrong boundary. The backend needs two different truth families: current file mapping (`cluster_map`, `data_length`, `valid_data_length`) and current mount runtime refusal state (`forced_shutdown`, `VolumeFlags`, read-only posture). Those two families do not share one safe long-held outer guard. The later June 24 design freeze also rejected payload-split follow-ons: reuse `PageCacheContext` directly, and allow only the mount-runtime small-lock payload as a new struct in this repair line. | Dispatch created at `.agents/subagent-tasks/xfstests_linux_baseline_20260624/pass_05_regular_file_page_cache_owner_split_creator_dispatch.md` on June 27, 2026. Source-of-truth design remains `.agents/tmp/20260624-regular-file-page-cache-owner-split-design.md`. User hard override copied into the dispatch: no new `struct`/`enum`/local alias/module/carrier/helper beyond the single mount-runtime small-lock payload on `ExfatFs`; Creator must stop and report if more structure seems required. |
| `pass_05_regular_file_page_cache_owner_split_creator_repair_01` | Creator Repair Pass | Narrow post-review repair for the first `pass_05` landing: synchronize `mount_runtime.forced_shutdown` at `volume.rs::set_forced_shutdown()`, and move the matching regular-file cached-I/O refusal reads in `inode/cached_io.rs::map_regular_file_logical_offset()` and `inode/cached_io.rs::read_at_impl()` onto the same `mount_runtime` truth source already used by `inode/page_backend.rs`. Do not widen beyond those refusal-path closures. | Main-agent review accepted the persistent `PageCacheContext` lifecycle core, but found the owner-split truth chain still incomplete: page-cache backend callbacks now trust `mount_runtime`, while the forced-shutdown writer and the two named cached-I/O refusal sites do not yet publish/read that same surface. Keep the repair narrow so the pass closes the truth-source split without reopening growth/shrink/page-cache publication logic. | Creator completed the repair at `.agents/components/xfstests_linux_baseline_20260624/pass_05_regular_file_page_cache_owner_split_creator_repair_01.md` on June 27, 2026. Main-agent review accepted the truth-source closure but found one remaining small-lock guard-lifetime defect in `inode/cached_io.rs`, so `repair_02` was opened immediately. Same user hard override remained in force throughout. |
| `pass_05_regular_file_page_cache_owner_split_creator_repair_02` | Creator Repair Pass | Narrow post-review repair for `repair_01`: in `inode/cached_io.rs::map_regular_file_logical_offset()` and `inode/cached_io.rs::read_at_impl()`, turn the new `mount_runtime` reads into copied short-lived snapshots instead of live `RwLockReadGuard`s that remain in scope across later mapping / page-cache work. Match the existing `inode/page_backend.rs` read shape and do not widen beyond those two lines of truth-source use. | Main-agent review of `repair_01` found the truth source itself was corrected, but the lock-lifetime shape still drifted from the June 24 design rule: small locks are publication surfaces and should not remain held across later work. Keep this repair extremely narrow so it closes only the guard-lifetime issue without reopening the refusal-source decision. | Creator completed the repair at `.agents/components/xfstests_linux_baseline_20260624/pass_05_regular_file_page_cache_owner_split_creator_repair_02.md` on June 27, 2026. Main-agent static review found no further packet-scoped structural gaps in the current owner-split lane. No compile preflight or Checker run has happened yet. |
| `pass_05_regular_file_page_cache_owner_split_checker_smoke_01` | Creator-Synced Pass | User-directed narrow Checker smoke gate for the repaired `pass_05` owner-split lane: rerun exactly `generic/313` and `generic/639` through the initramfs xfstests lane using `test/initramfs/src/conformance/xfstests/exfat_refactor_pass5_smoke_313_639.list`, preserve exact runtime receipts, and classify whether the repaired owner-split path now clears the primary first-write / remount-extension failure signals. If the first run still fails and existing logs are not decisive, Checker may add temporary production logging inside the packeted owner-split write-set and rerun the same two tests once, but must not change logic or open Creator from inside the pass. | The user explicitly requested that Checker open now and run only the two simple xfstests that anchor this repair line. The current owner-split lane has passed main-agent static review but has no compile/runtime receipt yet. This smoke gate intentionally stays narrower than a full final pass acceptance and exists to answer the immediate question "did the repaired path clear the two primary xfstests signals?" before broader validation is reopened. | Checker completed at `.agents/components/xfstests_linux_baseline_20260624/pass_05_regular_file_page_cache_owner_split_checker_smoke_01.md` with preserved receipts under `.agents/tmp/20260627-114117-pass_05_regular_file_page_cache_owner_split_checker_smoke_01/`. Both intended tests actually ran and both failed on the first run (`generic/313`: `write(2) failed: Invalid argument`; `generic/639`: two `pwrite: Invalid argument` lines). Logs were decisive, so Checker did not edit production `.rs` files, did not perform a logging rerun, and did not open Creator. The resulting repair batch re-localizes the remaining bug to the current live growth-path publication / mapping surface rather than the already-closed lexical-owner-split defect. |
| `pass_05_regular_file_page_cache_owner_split_checker_log_localization_02` | Creator-Synced Pass | User-corrected Checker-only direct-localization follow-up for the same repaired `pass_05` owner-split lane: reuse exactly `generic/313` and `generic/639`, but begin with temporary production logging in the current live growth-path publication / mapping surface and preserve read-only TEST/SCRATCH image triage from `test/initramfs/build/xfstests_test.img` and `test/initramfs/build/xfstests_scratch.img` if the logging run still leaves ambiguity. Checker must not change logic and must not open Creator from inside the pass. | The smoke gate already answered the binary question and proved the old owner-split fix is insufficient. The user explicitly rejected immediate Creator routing and instead wants one more bounded Checker pass that can directly distinguish backend-invalid cluster topology, premature `valid_data_length` publication, or post-remount on-disk publication mismatch by using production logging plus read-only image dump/triage. | Checker completed at `.agents/components/xfstests_linux_baseline_20260624/pass_05_regular_file_page_cache_owner_split_checker_log_localization_02.md` with preserved receipts under `.agents/tmp/20260627-120349-pass_05_regular_file_page_cache_owner_split_checker_log_localization_02/`. Both intended tests again ran and failed. The pass usefully narrowed the bug to the pre-remount first buffered-write path, but it did **not** capture the required runtime payload: temporary probes in `file_mutation.rs`, `page_backend.rs::write_page_async()`, and `cached_io.rs` produced no observable log lines in the guest receipts. Image triage was therefore skipped as unnecessary for this narrower classification, and the next single instrumentation point was escalated to `inode/page_backend.rs:148-152` in `read_page_async()` immediately before `regular_file_page_range(...)`. |
| `pass_05_regular_file_page_cache_owner_split_checker_read_page_reentry_localization_03` | Creator-Synced Pass | Second Checker-only localization follow-up for the same repaired `pass_05` owner-split lane: rerun exactly `generic/313` and `generic/639`, but limit temporary diagnostics to visible-severity probes at `inode/page_backend.rs::read_page_async()` and the `regular_file_page_range(...)` rejection inside `inode/cached_io.rs`. This pass exists only to prove or disprove the suspected partial-page refill reentry path before Creator reopens. | `log_localization_02` narrowed the failure family correctly but failed to surface any observable payload from its broader write-path probes. The next smallest discriminating experiment is to instrument only the suspected first buffered-write readback reentry path with `warn!`/`error!` severity so the guest receipts must either show the stale page-0 payload or prove that `read_page_async()` was not the first rejector. | Checker completed at `.agents/components/xfstests_linux_baseline_20260624/pass_05_regular_file_page_cache_owner_split_checker_read_page_reentry_localization_03.md` with preserved receipts under `.agents/tmp/20260627-121702-pass_05_regular_file_page_cache_owner_split_checker_read_page_reentry_localization_03/`. This pass proved the concrete rejector: on the first buffered write, `inode/page_backend.rs::read_page_async()` reaches `regular_file_page_range(...)` for `idx=0` while the callback-visible context still shows `first_cluster=0`, `no_fat_chain=false`, `data_length=0`, and `valid_data_length=0`. `inode/cached_io.rs` then rejects immediately on `file_offset=0 >= data_length=0`. The same stale zero-length payload appeared once for `generic/313` and twice for `generic/639`, proving the remaining live bug is stale zero-length page-cache context during the first partial-page refill reentry rather than a remount-only publication problem. |
| `pass_05_regular_file_page_cache_owner_split_creator_repair_03` | Creator Repair Pass | Narrow runtime-proven repair for the same `pass_05` owner-split lane: growth-path file mutation must publish a temporary callback-visible context with backend-valid new mapping plus new `data_length` but old `valid_data_length` before any callback-capable `page_cache.*` operation can re-enter `read_page_async()` on the grown file view; only after zero-fill/user-copy materialization succeeds may it promote `valid_data_length` to the final grown value. Apply the same staged-publish rule to ordinary write growth/append/gap-write and `resize_impl()` grow. | Checker `..._03` proved the current rejector line pair and showed that the existing publish in `inode/file_mutation.rs:475-482` is too late or otherwise not effective for the first partial-page refill reentry. The smallest principled repair is therefore not another owner split or remount change, but one narrow growth-path publication-order repair inside `meso_06_file_content_mutation`, while preserving the user's absolute no-new-entity constraint. | Creator completed at `.agents/components/xfstests_linux_baseline_20260624/pass_05_regular_file_page_cache_owner_split_creator_repair_03.md` on June 27, 2026. Main-agent static review accepted the repair as packet-compliant: write-path `page_cache.resize(...)` and `prepare_regular_file_page_cache_range(...)` now run only after provisional `{cluster_map = new, data_length = new, valid_data_length = old}` publication, final `valid_data_length` promotion happens only after materialization succeeds, `resize_impl()` grow inherits the same sequencing through `grow_and_commit_regular_file()`, and no forbidden new entities were introduced. The next step is a synchronized two-test Checker rerun. |
| `pass_05_regular_file_page_cache_owner_split_checker_smoke_04` | Creator-Synced Pass | Synchronized post-`creator_repair_03` Checker rerun for the same `pass_05` lane: rerun exactly `generic/313` and `generic/639` through the initramfs xfstests lane using `test/initramfs/src/conformance/xfstests/exfat_refactor_pass5_smoke_313_639.list`, preserve full runtime receipts, and determine whether the staged growth-path publication repair clears the first-write / EOF-extension `EINVAL` failures. If the first rerun still fails and logs are not decisive, Checker may add temporary production logging only inside the repaired growth-path surface and rerun the same two tests exactly once, but must not change logic or open Creator from inside the pass. | `creator_repair_03` is now statically accepted, but the repair still has no synchronized runtime proof. The user-approved narrow two-test gate remains the fastest way to confirm whether the staged callback-visible publication timing actually fixes the proven `read_page_async() -> regular_file_page_range(...)` rejector without broadening back out to aggregate xfstests batches. | Checker completed at `.agents/components/xfstests_linux_baseline_20260624/pass_05_regular_file_page_cache_owner_split_checker_smoke_04.md` on June 27, 2026, with preserved receipts under `.agents/tmp/20260627-125651-pass_05_regular_file_page_cache_owner_split_checker_smoke_04/`, `.agents/tmp/20260627-131149-pass_05_regular_file_page_cache_owner_split_checker_smoke_04_logging_rerun/`, and `.agents/tmp/20260627-131409-pass_05_regular_file_page_cache_owner_split_checker_smoke_04_logging_rerun_after_qemu_cleanup/`. The old `EINVAL` symptom changed materially: `generic/313` now reaches visible completion, while `generic/639` no longer prints `pwrite: Invalid argument` but instead stalls immediately after `QA output created by 639` in both real two-test runs. Temporary production logging did not surface any visible markers, so the pass is accepted as a runtime state change plus hang classification, but not as sufficient root-cause localization; the next step is one more Checker-only localization pass before reopening Creator. |
| `pass_05_regular_file_page_cache_owner_split_checker_639_hang_localization_05` | Creator-Synced Pass | User-directed `generic/639`-only localization pass for the same `pass_05` lane: rerun only `generic/639` through the initramfs xfstests lane using `test/initramfs/src/conformance/xfstests/exfat_refactor_pass5_639_only.list`, then use bounded observability to answer three exact questions: where the hang occurs, why that step hangs, and which production code line(s) own the real root cause. Temporary external xfstests step markers, temporary production logs, and read-only image dumps are allowed, but Creator routing remains forbidden from inside the pass. | `checker_smoke_04` already proved a material state transition: the old `EINVAL` moved out of `generic/313`, and `generic/639` now stalls in its silent region instead of failing with `pwrite: Invalid argument`. The next smallest decisive step is therefore not another Creator guess, but one more Checker-only localization pass that isolates the exact silent sub-step before any repair logic is reopened. | Checker completed at `.agents/components/xfstests_linux_baseline_20260624/pass_05_regular_file_page_cache_owner_split_checker_639_hang_localization_05.md` on June 27, 2026, with preserved receipts under `.agents/tmp/20260627-133752-pass_05_regular_file_page_cache_owner_split_checker_639_hang_localization_05/`, `.agents/tmp/20260627-133851-pass_05_regular_file_page_cache_owner_split_checker_639_hang_localization_05_userspace_markers/`, `.agents/tmp/20260627-133958-pass_05_regular_file_page_cache_owner_split_checker_639_hang_localization_05_console_markers/`, and `.agents/tmp/20260627-134411-pass_05_regular_file_page_cache_owner_split_checker_639_hang_localization_05_production_logs/`. This pass proved that the first 32-byte write reached durable publication and that the later 64-byte grown state never reached durable publication in the preserved failed image, so the remaining hang is narrowed to the mount-cycle / second-`pwrite` boundary before final readback/hexdump. However, neither temporary userspace markers nor temporary production logs surfaced in guest receipts, so the cited `page_cache_context` owner lines remain evidence-backed inference rather than directly logged runtime proof. |
| `pass_05_regular_file_page_cache_owner_split_checker_mount_cycle_localization_06` | Creator-Synced Pass | User-corrected real mount/remount-specific localization pass for the same `pass_05` lane: use the upstream initramfs xfstests environment in direct-command diagnostic mode to replay the `generic/639` sequence with forced visible boundaries around first write, `_test_unmount`, `_test_mount`, and second write. If needed, temporarily instrument upstream xfstests `common/rc` mount helpers and packet-authorized production sync/mapping surfaces, plus read-only failed-image dumps, until Checker can state where the mount-cycle stalls and which production line family truly owns it. | `checker_639_hang_localization_05` narrowed the failure to the mount-cycle / second-`pwrite` boundary but still did not produce any visible userspace-marker or production-log output. The next smallest decisive step is therefore to leave plain `./check` output behind and force explicit mount-cycle step visibility through direct-command mode before reopening Creator. | Checker completed at `.agents/components/xfstests_linux_baseline_20260624/pass_05_regular_file_page_cache_owner_split_checker_mount_cycle_localization_06.md` on June 27, 2026. The first launcher receipt only captured a `docker.sock` permission failure, a later interrupted rerun left a stale checker lock that main-agent cleared per protocol, and the decisive preserved rerun is `.agents/tmp/20260627-190316-pass_05_regular_file_page_cache_owner_split_checker_mount_cycle_localization_06_rerun3/`. Direct-command markers now prove the hang is inside `_test_unmount` at guest `umount /dev/vdc`, after `STEP_03_BEFORE_TEST_UNMOUNT` and before any remount or second-write marker. The actionable production owner family is the filesystem sync loop plus regular-file pending-sync / eviction path: `fs.rs:57-87`, `inode/sync.rs:28-52`, `inode/sync.rs:76-91`, `inode/mod.rs:333-346`, with supporting dirty-state owner `inode/state.rs:166-180`. This pass upgrades the prior inference to a concrete unmount/sync localization and makes the lane ready for a narrow Creator repair batch. |
| `pass_05_regular_file_page_cache_owner_split_creator_repair_04` | Creator Repair Pass | Narrow post-localization repair for the same `pass_05` lane: fix the regular-file unmount-time dirty-page quiesce path so `generic/639` can progress past `_test_unmount`. The repair may touch `inode/sync.rs` and `inode/file_mutation.rs`, with `fs.rs`, `inode/mod.rs`, and `inode/state.rs` only if a minimal owner-local adaptation is strictly required. Close the concrete page-cache API misuse where dirty cached data is treated with clean-page eviction semantics, and keep pending-sync range truth aligned enough that `ExfatFs::sync()` can observe quiescence after successful writeback. | The mount-cycle localization pass proved the remaining hang is no longer at remount or second write; it is inside `umount /dev/vdc`, where `ExfatFs::sync()` loops because the current regular-file sync / mutation path does not drain dirty page-cache state. Main-agent's ext2 contrast then established the strongest local coding mistake: `exfat_refactor` uses bare `evict_range(...)` at the exact points where dirty-page writeback or invalidate semantics are needed. The next smallest decisive step is therefore one narrow Creator repair for page-cache API selection and dirty-page quiesce semantics, while preserving the user's absolute no-new-entity rule. | Creator completed at `.agents/components/xfstests_linux_baseline_20260624/pass_05_regular_file_page_cache_owner_split_creator_repair_04.md` on June 27, 2026. Main-agent static review accepted the repair as packet-compliant: `inode/sync.rs` now bootstraps the current callback-visible context and uses that `data_length` truth source for dirty-page quiesce, then calls `flush_range(0..data_length)` instead of `evict_range(...)`; `inode/file_mutation.rs` now uses `flush_range(cache_mutation_range)` instead of `evict_range(...)` for the named overlapping dirty-window cleanup; no forbidden new entity was introduced; no `fs.rs`, `inode/mod.rs`, or `inode/state.rs` edit was required. The next step is a synchronized `generic/639`-only Checker rerun. |
| `pass_05_regular_file_page_cache_owner_split_checker_639_post_repair_07` | Creator-Synced Pass | Synchronized post-`creator_repair_04` rerun for the same `pass_05` lane: rerun exactly `generic/639` through the initramfs xfstests lane using `test/initramfs/src/conformance/xfstests/exfat_refactor_pass5_639_only.list`, preserve full runtime receipts, and determine whether the repaired dirty-page quiesce / page-cache API selection fix clears the unmount hang. If the first rerun is still ambiguous, Checker may perform one temporary production-logging rerun in the packeted sync/mutation surfaces and then stop with a concrete conclusion. | `creator_repair_04` is now statically accepted, but the repair still has no runtime receipt. The user's requested next gate is narrower than a broad smoke rerun: just `generic/639`, because that test directly exercises the repaired unmount-time writeback path and the later remount/second-write chain. | Checker completed at `.agents/components/xfstests_linux_baseline_20260624/pass_05_regular_file_page_cache_owner_split_checker_639_post_repair_07.md` on June 27, 2026. The first packeted 639-only rerun was already decisive: preserved receipts under `.agents/tmp/20260627-200030-pass_05_regular_file_page_cache_owner_split_checker_639_post_repair_07/` show `FSTYP=exfat_refactor`, `generic/639` actually ran, and the guest logs reached `Ran: generic/639`, `Passed all 1 tests`, and `All conformance tests passed.` No optional logging rerun was needed, no production files were edited, and the repaired live state is now runtime-verified PASS for `generic/639`. |
| `pass_05_regular_file_page_cache_owner_split_checker_313_post_repair_08` | Creator-Synced Pass | Complementary post-`creator_repair_04` confirmation rerun for the same `pass_05` lane: rerun exactly `generic/313` through the initramfs xfstests lane using `test/initramfs/src/conformance/xfstests/exfat_refactor_pass5_313_only.list`, preserve full runtime receipts, and determine whether the repaired dirty-page quiesce / page-cache API selection fix also clears the previously paired first-write growth test. Stop on one concrete live-state conclusion; do not widen into broader validation from this pass. | `generic/639` is now runtime-verified PASS, but the original failure pair for this lane was `generic/313` plus `generic/639`, and the user explicitly requested one more narrow confirmation that `generic/313` also passes on the same repaired live state. Keeping this as a one-test rerun preserves exact evidence without reopening a broader smoke batch or a new repair loop. | Checker completed at `.agents/components/xfstests_linux_baseline_20260624/pass_05_regular_file_page_cache_owner_split_checker_313_post_repair_08.md` on June 27, 2026. The first host-side attempt did not count because preserved `stderr.txt` showed a Docker permission failure before guest execution; the decisive rerun receipt is `.agents/tmp/20260627-201816-pass_05_regular_file_page_cache_owner_split_checker_313_post_repair_08_docker_escalated/`. Its preserved runlist contains exactly `generic/313`, and preserved `stdout.txt` plus `qemu.log` show `FSTYP=exfat_refactor`, `generic/313` actually ran, `Ran: generic/313`, `Passed all 1 tests`, and `All conformance tests passed.` No logging rerun and no production edits were needed, so the repaired live state is now runtime-verified PASS for both `generic/313` and `generic/639`. |

**Harness boundary:** The upstream initramfs xfstests lane must keep TEST and SCRATCH devices distinct and must carry `XFSTESTS_SCRATCH_DEV` into generated `local.config`. The June 24 harness edit makes the static fallback `local.config` honor `XFSTESTS_TEST_DEV`, `XFSTESTS_SCRATCH_DEV`, and `XFSTESTS_MOUNT_OPTIONS`, and `run_xfstests.sh` now refuses identical TEST/SCRATCH device paths before invoking upstream `./check`.

### `xfstests_classification_20260509`

**Decision:** `S1A` remains the named-xfstests classification wave for the current direct-boot path. The accepted no-scratch decision pass already proved the batch can start without `SCRATCH_DEV`, and the autosync-corrected receipt exported real testcase failures instead of the old duplicate-`DentryKey` crash. The filtered rerun over `generic/001`, `generic/007`, and `generic/013` replaced that old exported-failure surface with a non-exporting `/tmp/r`-phase `HANG / STALE STATE`. The observability-enabled rerun proved that EXIT-trap-only files are still insufficient because they never become host-visible when the run does not return, and the console-streaming rerun narrowed the stale point to visible `generic/001` filesystem-consistency output followed by a flat `check_stdout_bytes=233`. The next pass leaves the xfstests batch aside and reuses that same preserved test image in a fresh NixOS boot for one direct guest-side `fsck -t exfat_refactor /dev/vdb` probe. `generic/023` stays out of execution because it is a symlink-capability `notrun`, not a logic failure.

| Pass ID | Kind | Covered Boundary | Slicing Rationale | Decision State |
| :--- | :--- | :--- | :--- | :--- |
| `pass_04_s1a_named_batch_without_scratch_checker` | Meso Integration Pass | Reuse the already-passed `S0` gate and the same `S1A` whitelist `generic/001`, `generic/007`, `generic/013`, `generic/023`, but classify one narrower harness question only: can this exact batch start and preserve testcase receipts when the local config omits `SCRATCH_DEV` while keeping `TEST_DEV`, `TEST_DIR`, `SCRATCH_MNT`, and the current guest-side `xfstests-check` discovery/export path intact? The pass may make packet-scoped edits only under `test/nixos/tests/xfstests/`; it must not route repairs into `kernel/src/fs/fs_impls/exfat_refactor/` and must preserve exact evidence if upstream `check` still insists on scratch despite the testcase set not using it directly. | The prior conclusion "guest-side formatter compatibility is the next blocker" may be true for broader xfstests coverage, but it is not yet proven to be necessary for this first `S1A` whitelist. A dedicated no-scratch rerun is the smallest experiment that separates a real upstream-suite requirement from an avoidable repo-harness assumption. | Accepted as a classification receipt. Dispatch: `.agents/subagent-tasks/xfstests_classification_20260509/pass_04_s1a_named_batch_without_scratch_checker_dispatch.md`. Artifact: `.agents/components/xfstests_classification_20260509/pass_04_s1a_named_batch_without_scratch_checker.md`. Result: the batch starts without `SCRATCH_DEV`, so scratch formatting is not the current gate for `S1A`; the next blocker is the guest panic `two distinct DirDentry's with identical DentryKey` at `kernel/src/fs/vfs/path/dentry.rs:912`. Keep the no-scratch harness shape for reruns. |
| `pass_05_s1a_named_batch_no_scratch_revalidation_true_checker` | Meso Integration Pass | Reuse the already-passed `S0` gate, keep the accepted no-scratch `S1A` harness shape, and rerun the same `generic/001`, `generic/007`, `generic/013`, `generic/023` batch after one narrow production change only: `kernel/src/fs/fs_impls/exfat_refactor/inode/mod.rs` now returns `true` from `revalidate_exists()` and `revalidate_absent()` for directory dentries instead of `false`. This pass must classify whether the earlier `dentry.rs:912` duplicate-`DentryKey` panic disappears, and if not, preserve the next exact blocker with logs and any exported receipts. | The current diagnosis points at VFS dcache churn caused by always-invalid directory revalidation. A single rerun on the same no-scratch lane is the smallest way to confirm whether that hypothesis was the dominant cause before routing a broader VFS or exFAT repair wave. | Accepted as a negative classification receipt. Dispatch: `.agents/subagent-tasks/xfstests_classification_20260509/pass_05_s1a_named_batch_no_scratch_revalidation_true_checker_dispatch.md`. Artifact: `.agents/components/xfstests_classification_20260509/pass_05_s1a_named_batch_no_scratch_revalidation_true_checker.md`. Result: the batch still panics with `two distinct DirDentry's with identical DentryKey` at `kernel/src/fs/vfs/path/dentry.rs:912`, so the `revalidate_exists()/revalidate_absent() -> true` change is insufficient. Keep the no-scratch harness shape and route the next pass toward the duplicate-key publication path itself. |
| `pass_06_s1a_named_batch_duplicate_dentry_key_log_checker` | Meso Integration Pass | Reuse the already-passed `S0` gate and the accepted no-scratch `S1A` harness shape, then rerun the same `generic/001`, `generic/007`, `generic/013`, `generic/023` batch after temporary logging-only instrumentation aimed at the duplicate-`DentryKey` publication path. Logging may touch narrow VFS-path surfaces plus packeted `exfat_refactor` directory lookup / mutation / revalidation surfaces, but must not change logic. This pass must identify the two distinct `DirDentry` identities or publication paths that collide at `kernel/src/fs/vfs/path/dentry.rs:912`, preserve the exact log evidence, and stop with a narrowed blocker classification or a new visible blocker if the panic shape changes. | The revalidation-return-value hypothesis is now disproven. The smallest next experiment is to instrument the actual duplicate-key publication path and capture the competing dentry identities, so the following repair can target one concrete VFS/exFAT publication sequence instead of another blind rerun. | Accepted as a narrowed but incomplete negative classification receipt. Dispatch: `.agents/subagent-tasks/xfstests_classification_20260509/pass_06_s1a_named_batch_duplicate_dentry_key_log_checker_dispatch.md`. Artifact: `.agents/components/xfstests_classification_20260509/pass_06_s1a_named_batch_duplicate_dentry_key_log_checker.md`. Result: the duplicate-`DentryKey` panic still reproduces, and the preserved stack shows that cross-directory rename is the failing syscall class, but the code order in `DirDentry::rename` means this pass did not prove that `ExfatInode::rename_impl` or `rename_across_directories` had executed before the VFS invariant broke. Ordinary log macros also failed to preserve a complete pre-crash event chain, so another Checker pass must move the tracing burden fully into VFS and print a chronological path-to-crash record. |
| `pass_07_s1a_named_batch_vfs_crash_path_checker` | Meso Integration Pass | Reuse the already-passed `S0` gate and the accepted no-scratch `S1A` harness shape, then rerun the same `generic/001`, `generic/007`, `generic/013`, `generic/023` batch with VFS-only temporary tracing. The pass must preserve a chronological path from `Path::rename` entry through `DirDentry::rename` and any parent-dentry lookup/materialization steps to the final `write_lock_children_on_two_dentries(...).equal` abort, including enough identity payload to distinguish the two parent dentries. If ordinary log lines may still be lost before panic, the checker should use a narrow VFS-local trace dump immediately before abort. | The current gap is not “whether rename is involved” but “which VFS events and dentry identities directly lead to the invariant break.” A VFS-only crash-path trace is the smallest next experiment that can convert the current inference into a complete preserved execution narrative. | Accepted as a negative classification receipt. Dispatch: `.agents/subagent-tasks/xfstests_classification_20260509/pass_07_s1a_named_batch_vfs_crash_path_checker_dispatch.md`. Artifact: `.agents/components/xfstests_classification_20260509/pass_07_s1a_named_batch_vfs_crash_path_checker.md`. Result: all three VFS-only tracing reruns still panic at `write_lock_children_on_two_dentries(...).equal`, and none of the added chronological trace payloads survive into guest receipts, even after crash-site dump and direct `panic!("{}", prebuilt_string)` retries. The accepted evidence remains VFS-local: duplicate parent-directory `DentryKey` equality abort before any preserved proof of filesystem rename callback entry. |
| `pass_08_s1a_named_batch_vfs_crash_path_autosync_checker` | Meso Integration Pass | Reuse the already-passed `S0` gate, the accepted no-scratch `S1A` harness shape, and the current VFS-only tracing tree from `pass_07`, then rerun the same `generic/001`, `generic/007`, `generic/013`, `generic/023` batch after the runner-level direct-boot kernel autosync fix. This pass validates whether the current kernel under test is now definitely the rebuilt tracing tree, and whether the trace payload finally appears in preserved guest receipts. It must not widen the instrumentation surface unless it first proves the autosync path still failed to stage the current kernel. | The previous VFS tracing pass likely ran against a stale staged direct-boot kernel: guest receipts still reported the old `unreachable!` text and old line numbers even though the workspace source had already moved to a new `panic!` path. A single rerun after automating staged-kernel autosync is the smallest way to revalidate the current theory on a known-current kernel before spending more effort on new tracing or repair. | Dispatched. Dispatch: `.agents/subagent-tasks/xfstests_classification_20260509/pass_08_s1a_named_batch_vfs_crash_path_autosync_checker_dispatch.md`. Planned artifact: `.agents/components/xfstests_classification_20260509/pass_08_s1a_named_batch_vfs_crash_path_autosync_checker.md`. |
| `pass_09_s1a_named_batch_without_symlinks_checker` | Meso Integration Pass | Reuse the already-passed `S0` gate and the accepted no-scratch `S1A` harness shape, but rerun only `generic/001`, `generic/007`, and `generic/013` on the current direct-boot autosync runner; `generic/023` must not be executed in this pass because it is a symlink-capability `notrun`. | The `pass_08` receipt already proved the current kernel reaches export and that `generic/023` is a capability exclusion, not a production crash. The smallest useful follow-up is to rerun only the three actual failure cases on the current kernel so any remaining result surface can be tied cleanly to filesystem-consistency or mount-state bugs. | Accepted as a classification receipt. Dispatch: `.agents/subagent-tasks/xfstests_classification_20260509/pass_09_s1a_named_batch_without_symlinks_checker_dispatch.md`. Artifact: `.agents/components/xfstests_classification_20260509/pass_09_s1a_named_batch_without_symlinks_checker.md`. Result: the filtered whitelist stayed at `generic/001`, `generic/007`, and `generic/013`, with `generic/023` absent, but the old exported `pass_08` failure surface did not reappear. Instead the guest reached `/tmp/r` and then produced no `XFSTESTS_BATCH_EXPORT_*` markers or per-test exports before the still-running QEMU had to be terminated, so the current blocker is a new in-batch `/tmp/r`-phase `HANG / STALE STATE`. |
| `pass_10_s1a_runtime_observability_checker` | Meso Integration Pass | Reuse the already-passed `S0` gate, the accepted no-scratch `S1A` harness shape, and the filtered whitelist `generic/001`, `generic/007`, `generic/013`, then rerun once with guest-side observability enabled around `/tmp/r`: `xfstests-check` start/return markers, heartbeat logging, and PATH-injected wrappers for `mount`, `umount`, `findmnt`, `sync`, `mkfs.exfat`, and `fsck.exfat`. `generic/023` must remain excluded. | `pass_09` proved the regression now happens after `/tmp/r` starts but before export or panic. The smallest next experiment is to keep the failing test boundary unchanged and add bounded observability to distinguish a long-running userspace checker from a kernel-side livelock or a mount/fsck path that stops making forward progress. | Accepted as a classification receipt. Dispatch: `.agents/subagent-tasks/xfstests_classification_20260509/pass_10_s1a_runtime_observability_checker_dispatch.md`. Artifact: `.agents/components/xfstests_classification_20260509/pass_10_s1a_runtime_observability_checker.md`. Result: the filtered batch again reaches `/tmp/r` and stays live with full-CPU QEMU plus changing `test.img`, but none of the new observability becomes host-visible because the EXIT-trap export never fires. The current blocker is therefore a refined non-exporting `/tmp/r`-phase `HANG / STALE STATE`, and the next pass must surface progress before the blocking `wait \"$check_pid\"` / EXIT-trap boundary. |
| `pass_11_s1a_console_streaming_observability_checker` | Meso Integration Pass | Reuse the already-passed `S0` gate, the accepted no-scratch `S1A` harness shape, and the filtered whitelist `generic/001`, `generic/007`, `generic/013`, then rerun once with console-streaming observability: guest markers, heartbeat lines, wrapper start/end events, and live `check.stdout` tailing must all be written directly to the guest console while still preserving `/tmp/b/*` files. `generic/023` must remain excluded. | `pass_10` proved that observability stored only under `/tmp/b/*` is still invisible if the run never reaches the EXIT trap. The smallest next experiment is to keep the failing test boundary unchanged and stream the same evidence directly to `qemu.log` so the next checker can classify progress while the run is still alive. | Accepted as a classification receipt. Dispatch: `.agents/subagent-tasks/xfstests_classification_20260509/pass_11_s1a_console_streaming_observability_checker_dispatch.md`. Artifact: `.agents/components/xfstests_classification_20260509/pass_11_s1a_console_streaming_observability_checker.md`. Result: console streaming works and narrows the stale point past generic `/tmp/r` entry. The batch reaches visible `generic/001` `_check_generic_filesystem: filesystem on /dev/vdb is inconsistent` output, but then stalls with repeated `heartbeat ... check_stdout_bytes=233`, never reaches `after-wait-xfstests-check`, and never exports. The current blocker is therefore an in-batch stale state inside or immediately after the visible `generic/001` filesystem-consistency path. |
| `pass_12_s1a_same_image_fsck_probe_checker` | Meso Integration Pass | Reuse the already-passed `S0` gate and the preserved direct-boot image set from `s1a-named-batch-12-console-streaming`, but do not rerun the filtered xfstests batch. Instead, boot a fresh NixOS session that reuses that exact `test.img` and execute only `exfat_refactor_fsck_same_image_probe`, which runs one direct guest-side `fsck -t exfat_refactor /dev/vdb` probe while printing the mount state before/after, the `fsck` return code, and the full `fsck` output to the console. | `pass_11` narrowed the stale point to the post-`generic/001` filesystem-consistency path but still left the xfstests harness in the loop. The smallest next experiment is to reuse the exact same image and classify whether direct guest-side `fsck` on that image returns cleanly, reports inconsistency and exits, or reproduces the stale state outside the batch harness. | Accepted as a classification receipt. Dispatch: `.agents/subagent-tasks/xfstests_classification_20260509/pass_12_s1a_same_image_fsck_probe_checker_dispatch.md`. Artifact: `.agents/components/xfstests_classification_20260509/pass_12_s1a_same_image_fsck_probe_checker.md`. Result: the same-image probe completes quickly and does not reproduce a stale run by itself, but util-linux `fsck` returns `0` without invoking a real exFAT checker: `fsck: fsck.exfat not found; ignore /dev/vdb`. The current blocker is therefore a guest userspace / harness dispatch gap, not yet a filesystem verdict on the preserved image. |
| `pass_13_s1a_same_image_fsck_helper_probe_checker` | Meso Integration Pass | Reuse the already-passed `S0` gate and the same preserved `s1a-named-batch-12-console-streaming/test.img`, then rerun only `exfat_refactor_fsck_same_image_probe` after the harness is tightened to resolve and invoke the installed exFAT helper directly as `fsck.exfat -n`. The probe must print helper-resolution markers, mount state before/after, the helper return code, and the full helper output to the console. | `pass_12` proved the same-image NixOS probe itself does not stall, but it also proved that util-linux `fsck` in this guest path does not dispatch a real exFAT checker. The smallest next experiment is to keep the exact same image boundary and boot path while bypassing that dispatch gap so the next receipt captures a real exFAT-checker verdict. | Accepted as a decisive classification receipt. Dispatch: `.agents/subagent-tasks/xfstests_classification_20260509/pass_13_s1a_same_image_fsck_helper_probe_checker_dispatch.md`. Artifact: `.agents/components/xfstests_classification_20260509/pass_13_s1a_same_image_fsck_helper_probe_checker.md`. Result: the helper resolves and runs, and the preserved image returns `FSCK_PROBE_RC=4` with root/FAT metadata errors (`failed to read the fat entry of root`, `the start cluster of root is wrong`, `failed to follow the cluster chain of root`, `failed to verify root directory`). The current blocker is therefore concrete preserved-image corruption, not a harness stall or helper-dispatch gap. |
| `pass_14_s1a_same_image_block_probe_checker` | Meso Integration Pass | Reuse the already-passed `S0` gate and the same preserved `s1a-named-batch-12-console-streaming/test.img`, then boot one fresh NixOS session that runs only `exfat_refactor_same_image_block_probe`. This pass must capture guest `/dev/vdb` block-device observations for the same preserved image: `fstat`, `lseek(SEEK_END)`, `BLKSSZGET`, `BLKGETSIZE64`, raw boot-sector bytes, BPB-decoded exFAT geometry, the root-cluster FAT entry bytes, and the first root-directory cluster bytes. The checker must also collect the same fields on the host/container directly from the preserved image file and compare guest-versus-host values. | The paired `pass_13` and Linux-side container check contradict each other: guest `/dev/vdb` reports root/FAT corruption, while the exact same image file is Linux-clean. The smallest next experiment is to stay on the same image and classify whether guest `/dev/vdb` is exposing different bytes or different block-device semantics from the host view of the same file. | Accepted as a harness-classification receipt. Dispatch: `.agents/subagent-tasks/xfstests_classification_20260509/pass_14_s1a_same_image_block_probe_checker_dispatch.md`. Artifact: `.agents/components/xfstests_classification_20260509/pass_14_s1a_same_image_block_probe_checker.md`. Result: the host-side comparison payload was collected, but the guest probe never ran because shell-first NixOS did not resolve `python3` from the default PATH. This closes only as a bounded harness failure, not as a guest-vs-host block verdict. |
| `pass_15_s1a_same_image_block_probe_python_path_checker` | Meso Integration Pass | Reuse the already-passed `S0` gate, the same preserved `s1a-named-batch-12-console-streaming/test.img`, and the same bounded `exfat_refactor_same_image_block_probe`, but rerun after tightening the harness to invoke `/nix/var/nix/profiles/system/sw/bin/python3` explicitly. The checker must again collect guest `/dev/vdb` block-device observations and compare them against host/container reads of the same preserved image file. | `pass_14` already proved the comparison plan is sound and that the remaining blocker is only interpreter resolution inside the shell-first guest. The smallest next experiment is to keep the same image boundary and same probe semantics while fixing only the Python path so the guest-versus-host comparison can finally execute. | Accepted as the decisive current classification receipt. Dispatch: `.agents/subagent-tasks/xfstests_classification_20260509/pass_15_s1a_same_image_block_probe_python_path_checker_dispatch.md`. Artifact: `.agents/components/xfstests_classification_20260509/pass_15_s1a_same_image_block_probe_python_path_checker.md`. Result: guest `/dev/vdb` reports the correct device size through `lseek(SEEK_END)`, `BLKGETSIZE64`, and `BLKSSZGET`, but the first data read `os.pread(fd, 64, 0)` fails with `EINVAL`. The host reads offset `0` from the same preserved image file successfully. The current blocker is therefore guest block-device byte-read semantics at offset `0`, not image corruption or harness packaging. |
| `pass_16_s1a_ext2_blockdev_fsck_probe_checker` | Meso Integration Pass | Reuse the already-passed `S0` gate, but do not reuse the preserved exFAT image and do not rerun any xfstests batch. Instead, prepare one fresh direct-boot run directory and execute only `ext2_blockdev_fsck_probe`, which must format guest `/dev/vdb` as ext2 with `mkfs.ext2 -F` and then run `e2fsck -f -n /dev/vdb` inside the same fresh NixOS boot. The checker must capture the helper-path markers, mount state before/after, `mkfs.ext2` return code/output, `e2fsck` return code/output, and `EXT2_PROBE_DONE` from `qemu.log`. If guest `e2fsck` fails after guest `mkfs.ext2` succeeds, the checker must also inspect the resulting `test.img` from the preserved run directory on the host/container and record whether Linux-side `e2fsck -f -n` on that same image file or a loop-attached view reports clean or not. | `pass_15` proved that current guest `/dev/vdb` rejects a 64-byte read at offset `0` on a Linux-clean exFAT image, but that still leaves one open branch in the user's decision tree: is this a generic NixOS guest block-device path problem, or something specific to the exFAT helper/path combination? The smallest discriminating experiment is to stay on the same guest `/dev/vdb` route while switching the filesystem userspace tools to ext2 on a freshly created clean image. | Accepted as a control classification receipt. Dispatch: `.agents/subagent-tasks/xfstests_classification_20260509/pass_16_s1a_ext2_blockdev_fsck_probe_checker_dispatch.md`. Artifact: `.agents/components/xfstests_classification_20260509/pass_16_s1a_ext2_blockdev_fsck_probe_checker.md`. Result: on the current workspace/kernel, guest `mkfs.ext2 -F /dev/vdb` and guest `e2fsck -f -n /dev/vdb` both return `0`, so the current direct-boot `/dev/vdb` path is not generically broken for fsck-style ext2 userspace access. Because this control ran after the local block-device byte-I/O repair in the current workspace, it does **not** by itself prove that any remaining exFAT discrepancy is inherently exFAT-specific; the next discriminating follow-up must rerun the exFAT same-image helper/block probes on the same current kernel. |
| `pass_17_s1a_ext2_blockdev_fsck_probe_without_byte_io_fix_checker` | Meso Integration Pass | Keep the same bounded `ext2_blockdev_fsck_probe` control surface as `pass_16`, but first rebuild the current kernel after temporarily short-circuiting the local block-device byte-I/O bounce-buffer repair so non-sector-aligned block-device reads/writes again return `InvalidArgs`. Then prepare one fresh direct-boot run directory and run only `ext2_blockdev_fsck_probe`. The checker must capture the same `EXT2_PROBE_*` helper/mount/mkfs/fsck receipts and preserve the fresh run logs. | `pass_16` proved that ext2 is clean on the current kernel, but that run happened after the local block-device byte-I/O repair had already landed. The user now wants the direct control experiment that isolates whether that repair is exactly what makes guest ext2 `e2fsck` succeed. The smallest discriminating follow-up is to keep the guest test surface identical and change only the block-device byte-I/O implementation back to the old behavior. | Accepted as direct negative control evidence. Dispatch: `.agents/subagent-tasks/xfstests_classification_20260509/pass_17_s1a_ext2_blockdev_fsck_probe_without_byte_io_fix_checker_dispatch.md`. Artifact: `.agents/components/xfstests_classification_20260509/pass_17_s1a_ext2_blockdev_fsck_probe_without_byte_io_fix_checker.md`. Result: after rebuilding the kernel with the local byte-I/O repair short-circuited back to old `InvalidArgs` behavior, guest `mkfs.ext2 -F /dev/vdb` still returned `0` and guest `e2fsck -f -n /dev/vdb` still returned `0`. This is direct evidence against the local byte-I/O repair being the determining factor for ext2 fsck success on `/dev/vdb`. |
| `pass_18_s1a_ext2_and_exfat_fsck_strace_checker` | Meso Integration Pass | Rebuild the shell-first `root-base.img` after adding `strace` to the xfstests guest package set, then collect syscall-level fsck traces through the direct-boot guest path. This pass has two bounded scenarios only: (1) one fresh ext2 control run executing `ext2_blockdev_fsck_strace_probe` on `/dev/vdb` after `mkfs.ext2 -F`; (2) one exFAT same-image run executing `exfat_refactor_fsck_same_image_strace_probe` on `/dev/vdb` while reusing the preserved failing `test.img` from `s1a-named-batch-12-console-streaming`. The checker must preserve the full `strace` output blocks and explicitly classify the first visible difference in `pread64/read/lseek/ioctl` shape between guest `e2fsck` and guest `fsck.exfat`. | `pass_17` proved that ext2 success does not depend on the local byte-I/O repair, so the next useful question is no longer "does ext2 depend on that fix?" but "what does guest `e2fsck` actually do differently from guest `fsck.exfat` on `/dev/vdb`?" Guest syscall traces are the smallest direct evidence that can answer that without more speculative block-layer instrumentation. | Accepted as a shared tracing-blocker classification receipt. Dispatch: `.agents/subagent-tasks/xfstests_classification_20260509/pass_18_s1a_ext2_and_exfat_fsck_strace_checker_dispatch.md`. Artifact: `.agents/components/xfstests_classification_20260509/pass_18_s1a_ext2_and_exfat_fsck_strace_checker.md`. Result: the shell-first `root-base.img` rebuild succeeded and both bounded guest probes reached the exact `strace` binary path, but both ext2 and exFAT failed before any traced filesystem syscalls were emitted. In both cases the only preserved trace output is `PTRACE_SETOPTIONS: Input/output error`, so this pass did not establish an ext2-versus-exFAT syscall-shape difference; it isolated a shared guest `strace` / `ptrace` blocker that must be repaired before further syscall-level comparison is meaningful. |
| `pass_19_s1a_same_image_fsck_helper_probe_without_byte_io_fix_checker` | Meso Integration Pass | Keep the same preserved `s1a-named-batch-12-console-streaming/test.img` and the same bounded guest helper probe `exfat_refactor_fsck_same_image_probe`, but first rebuild the current kernel after temporarily short-circuiting the local block-device byte-I/O bounce-buffer repair so non-sector-aligned block-device reads and writes again return `InvalidArgs`. Then prepare one fresh direct-boot run directory, overwrite only that run's `test.img` with the preserved exFAT image, and run one guest `fsck.exfat -n /dev/vdb` probe. The checker must preserve the exact `FSCK_PROBE_*` helper, mount-state, return-code, and output markers and stop at that single error receipt. | `pass_18` proved that the current direct-boot lane cannot provide useful syscall traces because guest `strace` fails before any filesystem syscalls are emitted. The user-directed next question is narrower: if we temporarily remove the local byte-I/O repair that made `os.pread(fd, 64, 0)` stop failing in the Python probe, what exact error does guest `fsck.exfat -n` print on the same preserved image? Reusing the old same-image helper probe while changing only the local byte-I/O behavior is the smallest evidence-producing follow-up. | Accepted as direct negative-control evidence. Dispatch: `.agents/subagent-tasks/xfstests_classification_20260509/pass_19_s1a_same_image_fsck_helper_probe_without_byte_io_fix_checker_dispatch.md`. Artifact: `.agents/components/xfstests_classification_20260509/pass_19_s1a_same_image_fsck_helper_probe_without_byte_io_fix_checker.md`. Result: after rebuilding with the local byte-I/O repair temporarily short-circuited back to old `InvalidArgs` behavior, guest `fsck.exfat -n /dev/vdb` still returns `FSCK_PROBE_RC=4` with the same root/FAT metadata errors. This is direct evidence that the local byte-I/O bounce-buffer repair is not the determining factor for the preserved same-image helper discrepancy. |
| `pass_20_s1a_same_image_aligned_sector_probe_checker` | Meso Integration Pass | Keep the same preserved `s1a-named-batch-12-console-streaming/test.img`, but do not rerun xfstests and do not reopen non-aligned byte reads. Instead, prepare one fresh direct-boot run directory and run one guest-side aligned-sector probe that reads only 512-byte aligned sectors from `/dev/vdb`: sector `0`, the sector containing the root FAT entry, and the sector containing the first root-directory cluster. The checker must collect the exact same three sector payloads on the host/container from the preserved image file and compare guest-versus-host bytes explicitly. | Linux-side validation is now clean on the same image with both `exfatprogs 1.2.2` and the guest-matched `exfatprogs 1.2.8`, while guest `fsck.exfat -n /dev/vdb` still returns `RC=4`. `pass_19` also proved that the local byte-I/O bounce-buffer repair is not the deciding axis. The smallest remaining discriminating experiment is therefore to bypass non-aligned reads entirely and ask whether guest `/dev/vdb` returns the same aligned sector bytes as the host for the exact exFAT regions that `fsck.exfat` later complains about. | Dispatched. Dispatch: `.agents/subagent-tasks/xfstests_classification_20260509/pass_20_s1a_same_image_aligned_sector_probe_checker_dispatch.md`. Pending artifact: `.agents/components/xfstests_classification_20260509/pass_20_s1a_same_image_aligned_sector_probe_checker.md`. |
| `pass_21_s1a_same_image_fsck_helper_verbose_probe_checker` | Meso Integration Pass | Keep the same preserved `s1a-named-batch-12-console-streaming/test.img`, do not rerun xfstests, and do not widen into new block probes. Instead, prepare one fresh direct-boot run directory and run one guest-side verbose exFAT helper probe `fsck.exfat -n -vv /dev/vdb`, preserving the full verbose output block plus the helper return code and mount-state markers. | The user-directed next question is to see the helper's own verbose path rather than infer more from sector or syscall probes. The smallest next experiment is therefore to reuse the same bounded same-image helper route and change only the helper arguments from `-n` to `-n -vv`, preserving whatever extra exfatprogs debug output appears. | Dispatched. Dispatch: `.agents/subagent-tasks/xfstests_classification_20260509/pass_21_s1a_same_image_fsck_helper_verbose_probe_checker_dispatch.md`. Pending artifact: `.agents/components/xfstests_classification_20260509/pass_21_s1a_same_image_fsck_helper_verbose_probe_checker.md`. |
| `pass_22_linux_fsck_trace_and_single_syscall_probes` | Meso Integration Pass | Trace the preserved same-image Linux/container-side `exfatprogs 1.2.8` helper directly against a loop-attached copy of `s1a-named-batch-12-console-streaming/test.img`, then replay the suspicious guest `/dev/vdb` syscalls individually instead of as one long chain. The guest probes are intentionally isolated so one failing syscall cannot suppress later candidates. | `pass_21` was no longer the most discriminating next step once the Linux-side trace showed the real helper sequence. The right follow-up was to split the guest-side comparison into single-syscall receipts for the block-device layer. | Accepted as classification receipts. Linux trace archived at `.agents/tmp/20260511-linux-fsck-exfat-1.2.8-loop.strace`; guest probe receipts archived under `.agents/xfstests/logs/s1a-blkpbszget-probe-01/` and `.agents/xfstests/logs/s1a-root-fat-entry-pread-probe-01/`. Results: guest `BLKPBSZGET` returned `ENOTTY`; guest `pread(fd, 4, 1048592)` returned `EINVAL`; the earlier aligned-sector control still matches host bytes. |
| `pass_23_generic_006_directory_growth_republish_creator` | Creator-Synced Pass | `meso_04_directory_entry_mutation` repair for `generic/006`: directory cluster growth must republish the grown directory's own Stream Extension (`DataLength`, `ValidDataLength`, `FirstCluster`, `NoFatChain`) into its parent entry set with checksum-valid bytes before the grown map is visible to later directory entry publication. | The localized failure is not that an entry set crosses a cluster boundary; the preserved image shows `/permname.260/a` and `/permname.260/b` still advertise one-cluster `DataLength = 32768` and `ValidDataLength = 32768` while their FAT chains have grown to multiple clusters. The smallest principled repair is therefore a directory-growth commit path analogous to regular-file entry-set writeback, not a no-cross-cluster allocation workaround. | Dispatched. Dispatch: `.agents/subagent-tasks/xfstests_classification_20260509/pass_23_generic_006_directory_growth_republish_creator_dispatch.md`. Planned artifact: `.agents/components/xfstests_classification_20260509/pass_23_generic_006_directory_growth_republish_creator.md`. |

### `lock_structure_repair_20260506`

**Decision:** The accepted Architect audit and Loop A Designer spec-only artifact repair the lock/state substrate across `meso_01_mount_volume_state`, `meso_02_free_space_accounting_and_discard`, `meso_05_file_content_mapping_and_cached_io`, `meso_06_file_content_mutation`, and `meso_07_file_sync_and_persistence`. Under the May 6, 2026 user override, this wave uses Creator-owned `cargo-check` plus direct main-agent Reviewer, with no Checker, no ktest artifact, and no functional-test plan. Loop A is sliced into two serialized Creator cleanup passes so `fs.rs` / `bitmap.rs` mount-allocator state and `inode/*` inode-local state can be reviewed independently. Use the same live Creator for A2 after A1 is accepted when practical, but do not execute A2 before A1 lands.

| Pass ID | Kind | Covered Boundary | Slicing Rationale | Decision State |
| :--- | :--- | :--- | :--- | :--- |
| `pass_a1_lock_structure_repair_20260506_mount_allocator_state` | Creator Cleanup Pass | Loop A substrate for immutable mount publication versus Level-1 `mount_state`, concise Rust-facing names for mount policy / anomaly / shutdown state, and Level-4 `allocator` / `allocator_state` as the sleep-capable `AllocationBitmap` On-disk Structure Owner boundary. Low-risk identity `.map_err(Error::from)` cleanup may be included only where naturally local to `fs.rs` / `bitmap.rs`. | `ExfatFs` mount state and allocator authority are tightly coupled by mount publication, superblock/statfs, sync posture, and free-space truth, but they should land before inode-local state consolidation so later inode changes can consume stable mount/allocator names without also rewriting owner placement. | Accepted by direct main-agent Reviewer. Creator artifact `.agents/components/lock_structure_repair_20260506/pass_a1_lock_structure_repair_20260506_mount_allocator_state_creator.md`; cargo-check receipt `.agents/checker-runs/lock_structure_repair_20260506/20260506-103623-lock_structure_repair_20260506-pass_a1_mount_allocator_state_creator/summary.tsv`. Semantic split is accepted; final `state` -> `mount_state` field rename remains deferred. |
| `pass_a2_lock_structure_repair_20260506_inode_state_substrate` | Creator Cleanup Pass | Loop A substrate for `inode_state` as the Level-2 inode-local metadata / dirty / mutation-authority boundary; preparation for later regular-file `cluster_map` RCU-equivalent generation work without completing that RCU migration; concise Rust-facing names around inode-local state; `DirtyLevel` / `ExfatInodeDirtyState` documentation; low-risk identity `.map_err(Error::from)` cleanup may be included only where naturally local to `inode/*`. | Keep inode-local restructuring separate from `fs.rs` / allocator work. This pass should leave Loop B ready to convert regular-file `cluster_map` to immutable generations, but it must not absorb the RCU migration or mutation/sync simplification reserved for later loops. | Accepted after repair 01 by direct main-agent Reviewer. Initial artifact `.agents/components/lock_structure_repair_20260506/pass_a2_lock_structure_repair_20260506_inode_state_substrate_creator.md`; initial cargo-check receipt `.agents/checker-runs/lock_structure_repair_20260506/20260506-105009-lock_structure_repair_20260506-pass_a2_inode_state_substrate_creator/summary.tsv`; repair artifact `.agents/components/lock_structure_repair_20260506/pass_a2_lock_structure_repair_20260506_inode_state_substrate_creator_repair_01.md`; repair cargo-check receipt `.agents/checker-runs/lock_structure_repair_20260506/20260506-110555-lock_structure_repair_20260506-pass_a2_inode_state_substrate_creator_repair_01/summary.tsv`. |
| `pass_a2_lock_structure_repair_20260506_inode_state_substrate_repair_01` | Creator Repair Pass | Narrow A2 repair only for dirty-publication helper guard discipline. Covered boundary: preserve `inode_state` naming / dirty-state docs while ensuring dirty-publication helpers do not hide reentrant Level-2 acquisition under already-held `inode_state` guards. | This is a direct-main-agent Reviewer repair batch. It must not widen into Loop B RCU migration or Loop C mutation/sync simplification; it only makes A2's Level-2 authority model non-reentrant and auditable. | Accepted by direct main-agent Reviewer. `mark_content_publication_dirty()` / `mark_metadata_publication_dirty()` now require caller-held `&InodeStateWriteGuard<'_>` proof and no longer acquire `inode_state` internally. Independent Loop A Reviewer later rejected final closure; main-agent triage treats `metadata_projection()` / physical metadata-dirty consolidation as Loop B/C seams, but generic `Published*` / `Admitted*` carriers, final `state` spelling, and identity `.map_err(Error::from)` wrappers still need explicit routing. |
| `pass_a3_lock_structure_repair_20260506_loop_a_surface_cleanup` | Creator Cleanup Pass | Narrow Loop A surface cleanup after independent Reviewer rejection: final `ExfatFs::state` -> `mount_state` spelling, generic `Published*` / `Admitted*` carrier/method names, `AdmittedDirectoryContext::publication()`, and identity `.map_err(Error::from)` wrappers in the in-scope files. It must not absorb `metadata_projection()` simplification, RCU-equivalent `cluster_map` generations, physical `metadata` / `dirty_state` consolidation, or Loop C mutation/sync simplification. | The Reviewer findings split into real Loop B/C temporary seams and small Loop A surface debt. A3 exists so the small naming / thin-wrapper surface does not pollute Loop B/C packets or get silently waived. | Accepted by direct main-agent Reviewer. Creator artifact `.agents/components/lock_structure_repair_20260506/pass_a3_lock_structure_repair_20260506_loop_a_surface_cleanup_creator.md`; cargo-check receipt `.agents/checker-runs/lock_structure_repair_20260506/20260506-114344-lock_structure_repair_20260506-pass_a3_loop_a_surface_cleanup_creator/summary.tsv`; Reviewer artifact `.agents/components/lock_structure_repair_20260506/pass_a3_lock_structure_repair_20260506_loop_a_surface_cleanup_reviewer.md`. |
| `pass_b1_lock_structure_repair_20260506_cluster_map_generation_read_path` | Creator Cleanup Pass | Loop B single-pass implementation for RCU-equivalent immutable regular-file `cluster_map` generations and all read-side consumers: mapping helpers, cached read path, `PageCacheBackend`, `npages`, regular-file `metadata_projection()`, dead `read_regular_file_at` deletion, and removal of the A3 `published_lookup_state()` wrapper if its read-side callers are migrated. It may add the bounded writer-side generation replacement helper under caller-held `inode_state`, but must not absorb Loop C `write_at_impl()` / resize-shrink / `sync_regular_file()` control-flow simplification or deferred allocator reuse hookup. | The Loop B Designer contract intentionally has one coherent risk surface: every read-side consumer must move to the same immutable-generation substrate. Splitting substrate and readers would leave two parallel read paths, which the Designer spec rejects. | Accepted by direct main-agent Reviewer. Creator artifact `.agents/components/lock_structure_repair_20260506/pass_b1_lock_structure_repair_20260506_cluster_map_generation_read_path_creator.md`; cargo-check receipt `.agents/checker-runs/lock_structure_repair_20260506/20260506-124127-lock_structure_repair_20260506-pass_b1_cluster_map_generation_read_path_creator/summary.tsv`; Reviewer artifact `.agents/components/lock_structure_repair_20260506/pass_b1_lock_structure_repair_20260506_cluster_map_generation_read_path_reviewer.md`. |
| `pass_c1_lock_structure_repair_20260506_mutation_generation_publication` | Creator Cleanup Pass | Loop C mutation / resize publication core: simplify `write_at_impl()` and `resize_impl()` grow/shrink so they consume B1's immutable `cluster_map` generation substrate and publish complete replacement generations through caller-held `inode_state`, without ending in lock-drop-reacquire consistency loops around page-cache/backend/I/O. Removed clusters from shrink/truncate must not be released to ordinary allocator reuse; this pass may either add the narrow staging needed for conservative no-reuse or leave a precise deferred-retirement seam for C3. It must not absorb `sync_regular_file()` dirty-window simplification except for compile-local fallout. | This is the user-named core lock bug. It should land before sync cleanup because write/resize publication determines the file-visible generation and removed-cluster consequences that sync later consumes. Keeping sync out of C1 keeps review focused on mutation control flow and generation publication rather than dirty-clear semantics. | Accepted after repair 01 by direct main-agent Reviewer. Initial Creator artifact `.agents/components/lock_structure_repair_20260506/pass_c1_lock_structure_repair_20260506_mutation_generation_publication_creator.md`; initial cargo-check receipt `.agents/checker-runs/lock_structure_repair_20260506/20260506-132859-lock_structure_repair_20260506-pass_c1_mutation_generation_publication_creator/summary.tsv`; initial Reviewer artifact `.agents/components/lock_structure_repair_20260506/pass_c1_lock_structure_repair_20260506_mutation_generation_publication_reviewer.md`; repair artifact `.agents/components/lock_structure_repair_20260506/pass_c1_lock_structure_repair_20260506_mutation_generation_publication_creator_repair_01.md`; repair cargo-check receipt `.agents/checker-runs/lock_structure_repair_20260506/20260506-134403-lock_structure_repair_20260506-pass_c1_mutation_generation_publication_creator_repair_01/summary.tsv`; repair Reviewer artifact `.agents/components/lock_structure_repair_20260506/pass_c1_lock_structure_repair_20260506_mutation_generation_publication_repair_01_reviewer.md`. |
| `pass_c2_lock_structure_repair_20260506_sync_dirty_window` | Creator Cleanup Pass | Loop C sync dirty-window simplification: simplify `sync_regular_file()` for `sync_data()` / `sync_all()` so it admits one Level-2 dirty window and one immutable `cluster_map` generation, drives page-cache writeback / backend BIO planning / block-device sync from that admitted state, and clears only the dirty generation proven durable for that admitted window. It must not reopen public mount or same-inode `regular_file_cluster_map_snapshot()` after writeback or device sync. It must not absorb C3 allocator retirement / deferred reuse. | C2 follows C1 because sync consumes the file-visible generation and dirty state C1 now publishes. Keeping allocator retirement out preserves a focused review surface around dirty-window admission and sync failure maintenance. | Accepted by direct main-agent Reviewer. Creator artifact `.agents/components/lock_structure_repair_20260506/pass_c2_lock_structure_repair_20260506_sync_dirty_window_creator.md`; cargo-check receipt `.agents/checker-runs/lock_structure_repair_20260506/20260506-135715-lock_structure_repair_20260506-pass_c2_sync_dirty_window_creator/summary.tsv`; Reviewer artifact `.agents/components/lock_structure_repair_20260506/pass_c2_lock_structure_repair_20260506_sync_dirty_window_reviewer.md`. |
| `pass_c3_lock_structure_repair_20260506_deferred_cluster_reuse` | Creator Cleanup Pass | Loop C allocator retirement / deferred reuse hookup: clusters removed from a superseded regular-file `cluster_map` generation must become ordinary allocator-reusable only after that superseded generation is no longer reachable by readers. This pass may refine the B1/C1 generation substrate if needed so snapshots share a real generation lifetime rather than copying the map. It must route shrink removed ranges through allocator-owned retirement or a proven generation-retirement drain path, remove C1's permanent no-reuse placeholder, and preserve C1/C2 lock contracts. | True deferred reuse is a different risk surface from mutation/sync simplification. It touches Level-4 allocator truth and possibly the generation container lifetime, so it needs its own review instead of being folded into C1 or C2. | Accepted after repair 02 by direct main-agent Reviewer. Initial Creator artifact `.agents/components/lock_structure_repair_20260506/pass_c3_lock_structure_repair_20260506_deferred_cluster_reuse_creator.md`; initial cargo-check receipt `.agents/checker-runs/lock_structure_repair_20260506/20260506-142543-lock_structure_repair_20260506-pass_c3_deferred_cluster_reuse_creator/summary.tsv`; initial Reviewer artifact `.agents/components/lock_structure_repair_20260506/pass_c3_lock_structure_repair_20260506_deferred_cluster_reuse_reviewer.md`; repair 01 artifact `.agents/components/lock_structure_repair_20260506/pass_c3_lock_structure_repair_20260506_deferred_cluster_reuse_creator_repair_01.md`; repair 01 cargo-check receipt `.agents/checker-runs/lock_structure_repair_20260506/20260506-143457-lock_structure_repair_20260506-pass_c3_deferred_cluster_reuse_creator_repair_01/summary.tsv`; repair 01 Reviewer artifact `.agents/components/lock_structure_repair_20260506/pass_c3_lock_structure_repair_20260506_deferred_cluster_reuse_repair_01_reviewer.md`; repair 02 artifact `.agents/components/lock_structure_repair_20260506/pass_c3_lock_structure_repair_20260506_deferred_cluster_reuse_creator_repair_02.md`; repair 02 cargo-check receipt `.agents/checker-runs/lock_structure_repair_20260506/20260506-144842-lock_structure_repair_20260506-pass_c3_deferred_cluster_reuse_creator_repair_02/summary.tsv`; repair 02 Reviewer artifact `.agents/components/lock_structure_repair_20260506/pass_c3_lock_structure_repair_20260506_deferred_cluster_reuse_repair_02_reviewer.md`. |

**Deferred / Exit Notes:**

- Loop B is accepted through `pass_b1_lock_structure_repair_20260506_cluster_map_generation_read_path`. The remaining lock-structure work is Loop C.
- Loop C Designer, C1, C2, and C3 are accepted. The `lock_structure_repair_20260506` implementation wave is closed under the May 6, 2026 cargo-check-only / direct-main-agent-Reviewer override.
- Loop A is closed through A1, A2 repair 01, independent Reviewer triage, and A3 direct main-agent Reviewer. `metadata_projection()` and physical `metadata` / `dirty_state` consolidation remain Loop B/C seams, not A3 objectives.
- No `_designer_ktest.md`, Checker pass, ktest module, or functional-test obligation is scheduled for this wave unless the user changes the May 6, 2026 override.

### `state_entity_rename_refactor_20260507`

**Decision:** The accepted naming/refactor decisions recorded in Sections 6-12 of `.agents/main-agent/20260506-1600-state_entity_inventory_main_agent_handoff.md` are routed as one post-acceptance structural rename/refactor wave over already accepted exFAT refactor production code. Under the May 7, 2026 user override, only Creator dispatches are opened; each pass may run only the narrow `cargo-check` lane through `.agents/tools/checker_run.sh cargo-check ...`; no Checker, ktest, `make-kernel`, QEMU, or functional-test lane is scheduled; main-agent performs direct Reviewer acceptance. `InodeRewriteTarget` from handoff Section 10.5 is explicitly deferred and excluded from this wave. The wave is sliced into five serialized Creator cleanup passes so mount-state carrier surgery, stream/direntry renames, page-cache/dirty-state renames, allocator/lazy-reclaim ownership, and final lexical residue each stay reviewable on their own.

| Pass ID | Kind | Covered Boundary | Slicing Rationale | Decision State |
| :--- | :--- | :--- | :--- | :--- |
| `pass_01_state_entity_rename_refactor_mount_state_surface` | Creator Cleanup Pass | Post-acceptance mount-state carrier and mount-policy surface from handoff Sections 7, 8.7, 8.8, 10.3, 10.4, and the directly coupled generator/consumer renames in Section 11: delete `LookupMountSnapshot`; rename `LookupMountState` / `MutationMountState` to `MountStateReadGuard` / `MountStateWriteGuard`; remove `block_device` / `boot_region` from those carriers; rename `DirectoryMountAccess` to `MountAccessGuard`; rename `DirectoryMountState` to `MountStateAccessMode`; replace `DirectoryContextMode` with `is_write: bool`; rename `VolumeAnomalyState` to `VolumeFlags`; rename `ExfatMountOptions` to `MountOptions`. Compile-local mount vocabulary fallout from Section 12 may be included only where naturally required by the touched files. Do not absorb allocator ownership merge, stream/direntry structural renames, page-cache/dirty-state renames, or the broad final lexical sweep. | This is the highest-coupling read/write admission surface, but it is still one conceptual boundary: mount guard carriers and their direct consumers. Landing it first gives later passes stable mount-side names and removes the over-locked immutable fields before inode/direntry and sync-side cleanup begin. | Creator completed; pending direct main-agent Reviewer. Dispatch: `.agents/subagent-tasks/state_entity_rename_refactor_20260507/pass_01_state_entity_rename_refactor_mount_state_surface_creator_dispatch.md`. Creator artifact: `.agents/components/state_entity_rename_refactor_20260507/pass_01_state_entity_rename_refactor_mount_state_surface_creator.md`. Passing cargo-check receipt: `.agents/checker-runs/state_entity_rename_refactor_20260507/20260507-131821-state_entity_rename_refactor_20260507-pass_01_state_entity_rename_refactor_mount_state_surface_creator/summary.tsv`. |
| `pass_02_state_entity_rename_refactor_stream_and_direntry_surface` | Creator Cleanup Pass | Stream/direntry structural renames from handoff Sections 6, 8.2, 8.3, 9.4, 10.2, 10.6, 10.9, and the coupled field/getter renames in Section 11: `RepublishedFileEntrySet` to `MutableFileEntrySet`; `ExfatInodeClusterMap` to `StreamExtensionDirEntry`; `RegularFileClusterMapGeneration` to `ClusterMap`; delete `FileEntryClusterMap` into `StreamExtensionDirEntry`; rename `WritableDirectoryEntrySlotSpan` to `MutableDirEntrySlotSpan`; rename `DirectoryEntryAnomalyKind` to `DirEntryIssueKind`; apply compile-local `DirectoryEntry` / `ScannedDirectoryEntry` shortening only where naturally required by the touched direntry/stream surfaces. Do not absorb page-cache/dirty-state renames, allocator/lazy-reclaim ownership, or the broad pass05 lexical residue sweep. | `inode/state.rs`, `direntry.rs`, `inode/file_mutation.rs`, `inode/dir_mutation.rs`, and `inode/lookup.rs` all depend on one another here. Splitting type renames away from the byte-conversion merge would leave parallel stream/direntry vocabularies live at once. | Creator completed; pending direct main-agent Reviewer. Creator artifact: `.agents/components/state_entity_rename_refactor_20260507/pass_02_state_entity_rename_refactor_stream_and_direntry_surface_creator.md`. Passing cargo-check receipt: `.agents/checker-runs/state_entity_rename_refactor_20260507/20260507-134113-state_entity_rename_refactor_20260507-pass_02_state_entity_rename_refactor_stream_and_direntry_surface_creator/summary.tsv`. |
| `pass_03_state_entity_rename_refactor_page_cache_dirty_state_and_sync_surface` | Creator Cleanup Pass | Page-cache / dirty-state / sync vocabulary from handoff Sections 8.1, 8.4, 10.1 (`RegularFilePageCacheGuard` side only), and the related Section 11 / compile-local Section 12C and 12E fallout: `RegularFilePageCacheState` to `PageCacheContext`; `RegularFilePageCacheGuard` to `PageCacheContextGuard`; `ExfatInodeDirtyState` to `InodeDirtyState`; `install_regular_file_page_cache_state()` / `active_regular_file_page_cache_state()` / backend `regular_file_page_cache_state()` renames; dirty-state `publish` terminology to `mark_dirty` / `commit`; regular-file entry-set republish helper names to `write_back` / `commit`. Do not absorb allocator/lazy-reclaim work, mount guard carrier cleanup, or stream/direntry structural merge work. | The page-cache and dirty-state renames cross `inode/page_backend.rs`, `inode/sync.rs`, `inode/file_mutation.rs`, and `inode/state.rs` as one coherent regular-file runtime surface. Keeping this separate avoids mixing sync semantics review with allocator or direntry restructuring. | Creator completed; pending direct main-agent Reviewer. Creator artifact: `.agents/components/state_entity_rename_refactor_20260507/pass_03_state_entity_rename_refactor_page_cache_dirty_state_and_sync_surface_creator.md`. Passing cargo-check receipt: `.agents/checker-runs/state_entity_rename_refactor_20260507/20260507-142014-state_entity_rename_refactor_20260507-pass_03_state_entity_rename_refactor_page_cache_dirty_state_and_sync_surface_creator/summary.tsv`. |
| `pass_04_state_entity_rename_refactor_allocator_lazy_reclaim_and_cluster_guard` | Creator Cleanup Pass | Allocator ownership and deferred-reclaim surface from handoff Sections 6, 8.5, 10.1 (`ClusterAllocGuard` side), 10.7, and the related Section 11 / compile-local Section 12B fallout: `RetiredClusterRanges` to `LazyReclaimedCluster`; delete `AllocationBitmapState` by merging it into `AllocationBitmap`; rename `AllocationBitmapUpdate` to `BitmapOp`; introduce `ClusterAllocGuard` to replace `AllocatedDirectoryClusterGuard` and cover the regular-file growth rollback hole; rename allocator release/retire methods and `_with_publication` allocation function names only where needed by this ownership move. Do not absorb page-cache/dirty-state or stream/direntry cleanup. | This pass changes the allocator owner boundary and deferred reuse substrate, so it deserves its own review lane rather than being hidden inside naming-only passes. It also naturally follows the earlier mount and regular-file vocabulary stabilization. | Accepted by direct main-agent Reviewer with no code edits. Creator artifact: `.agents/components/state_entity_rename_refactor_20260507/pass_04_state_entity_rename_refactor_allocator_lazy_reclaim_and_cluster_guard_creator.md`. Passing cargo-check receipt: `.agents/checker-runs/state_entity_rename_refactor_20260507/20260507-143758-state_entity_rename_refactor_20260507-pass_04_state_entity_rename_refactor_allocator_lazy_reclaim_and_cluster_guard_creator/summary.tsv`. |
| `pass_05_state_entity_rename_refactor_lexical_residue_sweep` | Creator Cleanup Pass | Remaining non-structural lexical normalization from handoff Sections 10.9, 11 global lexical changes, and Section 12 after passes 01-04 land: surviving `DirectoryEntry` / `ScannedDirectoryEntry` shortening, residual `publish` / `publication` / `admitted` / `republish` word-root cleanup, wrapper deletions that were deferred only to keep earlier passes bounded, and final local variable / error-string normalization. `InodeRewriteTarget` remains out of scope and must stay deferred. | The first four passes remove the real owner/type boundaries. Keeping the broad lexical sweep last prevents noisy search-and-replace churn from obscuring the structural review of the earlier passes. | Accepted by direct main-agent Reviewer with no code edits. Creator artifact: `.agents/components/state_entity_rename_refactor_20260507/pass_05_state_entity_rename_refactor_lexical_residue_sweep_creator.md`. Passing cargo-check receipt: `.agents/checker-runs/state_entity_rename_refactor_20260507/20260507-145518-state_entity_rename_refactor_20260507-pass_05_state_entity_rename_refactor_lexical_residue_sweep_creator/summary.tsv`. |

**Deferred / Exit Notes:**

- `InodeRewriteTarget` stays deferred until a later directory/regular-file operation structure pass; this wave must not absorb it.
- Passes 01-05 are intentionally serialized. Pass05 in particular assumes passes 01-04 have already fixed the structural owner/type boundaries that the lexical cleanup refers to.
- No Checker pass is paired with this wave under the current user override. Creator-owned `cargo-check` receipts are the only scheduled execution evidence unless the user changes that rule.

### `meso_01_mount_volume_state`

**Decision:** Slice the full mount-volume-state meso contract into one broad implementation pass, follow-up structural cleanup passes, and one independent meso integration Checker pass.

| Pass ID | Kind | Covered Boundary | Slicing Rationale | Decision State |
| :--- | :--- | :--- | :--- | :--- |
| `pass_01_mount_volume_state` | Creator-Synced Pass | Mount-time boot validation, allocation bitmap truth source, volume flags, up-case table load, mount options, statfs counters, root inode exposure, and recount fallback. | The initial bootstrap needed the coherent mount state surface to typecheck and expose a usable VFS mount point. | Accepted. |
| `pass_01_mount_volume_state_cleanup_01` | Creator Cleanup Pass | Structural cleanup over the pass01 production surface. | Reviewer found `ondisk.rs` and owner-placement debt after the first implementation pass. | Rejected by Reviewer; superseded by later cleanup passes. |
| `pass_01_mount_volume_state_cleanup_02` | Creator Cleanup Pass | Continued owner-placement cleanup for pass01. | Cleanup stayed scoped to the already-implemented mount-volume-state slice, not new features. | Checker found compile failures; superseded by `cleanup_03`. |
| `pass_01_mount_volume_state_cleanup_03` | Creator Cleanup Pass | Continued owner-placement cleanup for pass01. | Follow-up cleanup preserved the same covered-micro boundary while closing residual structure issues. | Checker passed; later structural cleanup still required. |
| `pass_01_mount_volume_state_cleanup_04` | Reviewer / Creator Cleanup Pass | Final removal of `ondisk.rs` production-path debt and test-support surface confirmation. | Structural debt was still not fully closed, so Reviewer findings reopened Creator cleanup instead of direct Reviewer topology edits. | Final Checker passed; accepted. |
| `pass_01_mount_volume_state_user_named_structural_cleanup_05` | Creator Cleanup Pass | User-named surviving mount surface: `test_support/boot_region.rs::read_device_bytes`, stale notes `ValidatedMount` / `PublishedMountState`, surviving `MountedVolumeState`, `MountVolumeStateError`, removed `MountVolumeStateTarget` / `MountVolumeStateOperation` / `MountVolumeStateOutcome`, removed `mount_volume_state`, surviving owner-local `mount_candidate` / `remount_published`, and mount-related `#[cfg(ktest)] mod tests` / test-only support placement. | The April 21 problem note names mount-state carriers and helpers that cannot be repaired inside a `meso_02` free-space-only pass. The parent meso boundary must stay `meso_01`; the pass must use default-reject proof for carriers, helper families, thin helpers, and packeted test surfaces. | Reviewer approved with line-level-only seam documentation; final Checker skippable. This pass is accepted through Reviewer. |
| `pass_02_mount_volume_state_integration` | Meso Integration Checker Pass | Integration tests declared by the meso Designer for mounted state, statfs, bitmap/upcase loading, and recount fallback. | Integration obligations are Checker-owned and intentionally separate from Creator-synced pass validation. | PASS. |

**Deferred / Exit Notes:**

- No further mount-volume-state pass is currently sliced.
- Any future mount-volume-state reopening must explicitly state whether it is new feature work, cleanup over surviving entities, or integration-only validation.

### `meso_02_free_space_accounting_and_discard`

**Decision:** Slice the meso contract into an initial accounting-only core pass, pass01-only cleanup passes, one online-discard policy pass, one administrative `FITRIM` boundary pass, and one independent meso integration Checker pass.

| Pass ID | Kind | Covered Boundary | Slicing Rationale | Decision State |
| :--- | :--- | :--- | :--- | :--- |
| `pass_01_free_space_accounting_and_discard_core` | Creator-Synced Pass | Allocation bitmap as durable free-space truth source; cached cluster accounting through superblock/statfs; recount fallback under corruption-sensitive conditions. | The accounting lane is required before later discard policy can safely consume free-space state, while discard / `FITRIM` policy remains separable. | Accepted through Reviewer. |
| `pass_01_free_space_accounting_and_discard_core_cleanup_01` | Creator Cleanup Pass | Same pass01 accounting-only boundary; cleanup objective Line A was owner/helper placement. | Reviewer/user feedback identified structural debt in the pass01 implementation, but the first cleanup packet bundled multiple objectives too coarsely. | Partial closure only; do not treat as Checker-ready. |
| `pass_01_free_space_accounting_and_discard_core_cleanup_02` | Creator Cleanup Pass | Same pass01 accounting-only boundary; cleanup objective Line B was return-carrier consistency plus temporary error-seam closure. | This continuation existed because `cleanup_01` visibly closed owner/helper work but left return-carrier and error-boundary decisions open. | Accepted through Reviewer; final checker skipped after comment-only review edit. |
| `pass_02_free_space_accounting_and_discard_online_discard_policy` | Creator-Synced Pass | `Online discard is opportunistic and can downgrade at runtime`; the post-free path remains subordinate to `Allocation bitmap is the free-space truth source` and `Superblock counters and statfs reflect cached cluster accounting`. | Online discard is advisory and depends on correct free-space commit semantics from pass01, but it must not wait for administrative `FITRIM` because post-free downgrade policy is a distinct runtime path. | Creator artifact present; a user-directed full-surface Reviewer structural audit now runs before Checker. |
| `pass_02_free_space_accounting_and_discard_online_discard_policy_structural_audit_01` | Reviewer Pass | Same pass02 covered-micro boundary, plus a full-surface audit of every production `struct`, `enum`, carrier, and non-trait helper in `fs.rs` / `bitmap.rs`. | The user explicitly called out repeated helper / `struct` omissions in `fs.rs`, so this audit forces symbol-by-symbol disposition of surviving entities before the pass can continue. | Dispatched; does not replace the ordinary post-Checker Reviewer gate if pass02 later survives Checker unchanged. |
| `pass_02_free_space_accounting_and_discard_online_discard_policy_cleanup_01` | Creator Cleanup Pass | Same pass02 covered-micro boundary; fix the rejected free-space helper-family placement, dormant trim carriers, and registration panic surface. | The structural audit rejected the pass on three individuated objectives, so the next route must be a Creator cleanup pass rather than Checker. | Creator artifact present; Reviewer follow-up dispatched to verify structural closure before Checker. |
| `pass_02_free_space_accounting_and_discard_online_discard_policy_user_named_structural_cleanup_02` | Creator Cleanup Pass | User-named surviving free-space surface: moved `ClusterRange`, renamed `FreeSpaceAllocatorState`, `FreeSpaceSnapshot`, surviving `FreeSpaceAccountingOutcome`, `FreeSpaceAccountingOperation`, `FreeSpaceAccountingError`, `free_space_accounting_and_discard`, free-space helper family placement, `AllocationBitmap` / absent `BitmapInner` naming and owner boundary, `bitmap.rs` dependence on `MountVolumeStateError`, and free-space-related tests / test-only support placement. | The April 21 problem note shows `cleanup_01` was too narrow: it closed the previous three objectives but did not force default-reject proof for the surviving carrier family and owner naming. This pass must not absorb mount-state carrier cleanup; that belongs to `meso_01`. | Reviewer rejected for structural cleanup: surviving `FreeSpaceAccountingOperation / Outcome`, `free_space_accounting_and_discard`, the `ClusterRange` re-export seam, the `bitmap.rs` `MountVolumeStateError` seam, and the inline `fs.rs` ktest module remain open. Route back to Creator cleanup. |
| `pass_02_free_space_accounting_and_discard_online_discard_policy_user_named_structural_cleanup_03` | Creator Cleanup Pass | Reviewer-rerouted residual free-space surface: surviving `FreeSpaceAccountingOperation / Outcome`, surviving `free_space_accounting_and_discard`, the `ClusterRange` re-export seam, the `bitmap.rs` `MountVolumeStateError` seam, and the inline `fs.rs` `#[cfg(ktest)] mod tests` topology. | The tightened Reviewer protocol converted the broad “cleanup looks better” wave into explicit residual blockers. This follow-up pass should close only those named residuals instead of reopening already accepted owner placement work. | Reviewer approved with no code edits; final Checker skippable. This pass is accepted through Reviewer. |
| `pass_03_free_space_accounting_and_discard_admin_trim_boundary` | Creator-Synced Pass | `FITRIM and online discard are distinct administrative free-space paths`; current Asterinas-local `EOPNOTSUPP` fast-fail boundary; no allocator/counter mutation on trim rejection. | The Designer records that Asterinas currently lacks a dedicated VFS `FITRIM` hook and lower trim primitive, so this pass owns the meso-local operation/outcome boundary and explicit unsupported result without smuggling ioctl routing or privilege checks from `meso_11`. | Accepted through Reviewer on April 22, 2026; final Checker skippable because Reviewer made no edits. |
| `pass_04_free_space_accounting_and_discard_integration` | Meso Integration Checker Pass | Cross-pass validation for allocation/free/cached reporting, online-discard downgrade, `FITRIM` fast-fail distinction, recount failure preservation, repeated snapshots, and lock-linearized allocator observations. | Integration obligations in the Designer ktest must stay Checker-owned and run only after the relevant implementation passes exist. | Initial Checker run failed before the intended assertions because `bitmap.rs` wrote a non-sector-aligned `1984`-byte slice and returned `DeviceIo`; the lane is intentionally superseded by `pass_01_free_space_accounting_and_discard_core_cleanup_03` plus `pass_04_free_space_accounting_and_discard_integration_rerun_01`. |
| `pass_01_free_space_accounting_and_discard_core_cleanup_03` | Creator Cleanup Pass | Allocation/free bitmap writeback repair for the accepted pass01 core boundary after integration exposed `DeviceIo` on non-sector-aligned bitmap writes. | `pass_04` integration failed before reaching its real assertions because `bitmap.rs` wrote `1984` bytes through a sector-alignment-requiring block-device path. Route the Checker repair batch back to the narrowest existing owner boundary: pass01 core allocation/free accounting. | Creator landed the aligned writeback repair and the synchronized Checker pass then cleared the blocker; use this pass only as the repair lane, not as a substitute for the dedicated integration acceptance lane. |
| `pass_01_free_space_accounting_and_discard_core_cleanup_03_checker` | Creator-Synced Checker Pass | Regression proof that the aligned bitmap writeback repair removes the allocate/free `DeviceIo` blocker on the pass01 accounting path. | Use the exact failing `pass_04` reproduce commands as regression receipts without collapsing the dedicated integration lane into this synchronized pass. | Checker PASS; the allocate/free `DeviceIo` blocker is cleared and the acceptance proof continues under `pass_04_free_space_accounting_and_discard_integration_rerun_01`. |
| `pass_04_free_space_accounting_and_discard_integration_rerun_01` | Meso Integration Checker Pass | Full rerun of the four `pass_04` integration obligations after `cleanup_03` cleared the bitmap writeback `DeviceIo` blocker. | The first two integration tests passed under the synchronized cleanup checker, but the evidence had to be re-collected under the dedicated integration lane and include the two previously blocked obligations. | Checker PASS. |
| `pass_04_free_space_accounting_and_discard_integration_reviewer` | Reviewer Pass | Narrow final test-surface quality gate for the already-passing `pass_04` integration ktests and their helper support. | The integration lane became runtime-complete after the rerun; Reviewer then approved the touched test-only surface with line-level-only formatting/import edits. | Reviewer APPROVED; final Checker skippable; `pass_04` accepted. |

**Cleanup Objective Closure:**

- **Line A:** Owner/helper structural cleanup closed by promoting `AllocationBitmapRecord` to `AllocationBitmap`, moving bitmap helper behavior under that owner boundary, and removing helper-only `LogicalClusterRange`.
- **Line B:** Return-carrier and temporary error-seam cleanup closed by using a pass-local `FreeSpaceAccountingError` across the accounting/reporting/recount lane and making the mount/bootstrap conversion seam explicit.

**Deferred / Exit Notes:**

- Pass02 must commit free-space correctness before any discard hint and must downgrade / disable future discard attempts without rolling back bitmap truth or cached counters.
- Pass02 is also the latest acceptable point to confirm whether `FreeSpaceAccountingError` is promoted from pass-local boundary to the stable meso-owned error surface for widened discard behavior.
- Pass03 must keep administrative `FITRIM` distinct from online discard. Under current Asterinas interfaces, an explicit `EOPNOTSUPP` boundary is a valid implementation result; it must not silently pretend real trim I/O exists.
- Pass03 may define meso-local operation/outcome carriers for administrative trim admission, but carrier-specific ioctl routing, privilege checks, and user ABI remain outside this pass unless the main agent later schedules `meso_11` coordination.
- Neither pass02 nor pass03 may reuse `MountVolumeStateError` as a broad shared enum. Any temporary seam needs an explicit exit plan before the pass can reach Reviewer.
- Structural cleanup after pass02 or pass03 is not pre-sliced. If Reviewer finds helper-family, owner-placement, return-carrier, or error-seam debt, the main agent must open a named cleanup pass with individuated objectives before continuing toward integration.
- User-directed full-surface structural audits are separate from ordinary cleanup packets: they exist to force explicit disposition of every surviving helper / `struct` in the named write-set when generic cleanup wording proved too weak.

### `meso_03_directory_lookup_and_identity`

**Decision:** Slice the meso contract into one durable-structure-backed lookup Creator pass, one durable-structure-backed directory enumeration Creator pass, one independent meso integration Checker pass, and one post-acceptance lock-boundary cleanup pass. The slicing vocabulary may cite inherited Asterinas VFS directory surfaces, but it must not create or freeze new Rust signatures, request / outcome carrier names, or dispatcher families.

| Pass ID | Kind | Covered Boundary | Slicing Rationale | Decision State |
| :--- | :--- | :--- | :--- | :--- |
| `pass_01_directory_lookup_and_identity_lookup_resolution` | Creator-Synced Pass | Lookup resolution grounded in two durable inputs: the validated Up-case Table On-disk Structure Owner published by `meso_01_mount_volume_state`, and this directory's checksum-validated `DirectoryEntrySet` material. Covers mounted codec selection, Up-case folding, `NameHash` as prefilter only, trailing-dot-sensitive folded-name comparison under `keep_last_dots`, contiguous primary / secondary entry-set validation, location-derived child identity from the matched entry-set location, alias-equivalent spelling coherence, create-oriented stale-negative revalidation, and separation among absence, invalid caller spelling, and integrity / I/O failure on the inherited lookup surface. | The first implementation pass should make ordinary lookup correct as one coherent durable-structure pipeline. Splitting Up-case Table consumption, DirectoryEntrySet validation, location-derived identity, alias coherence, and stale-negative revalidation into separate early passes would either freeze artificial helper seams or leave a Checker with only partial, non-user-meaningful lookup behavior. | Checker rerun 02 PASS confirmed. Reviewer found a structural `iocharset` lookup seam in `inode.rs`, but user explicitly decided to absorb that seam cleanup into `pass_02_directory_lookup_and_identity_readdir_visibility_boundary` and treat `pass_01` as accepted. |
| `pass_02_directory_lookup_and_identity_readdir_visibility_boundary` | Creator-Synced Pass | Directory enumeration grounded in the same checksum-validated `DirectoryEntrySet` material and the same location-derived identity rules from `pass_01`: scan-order traversal, traversal-progress semantics on the inherited enumeration surface, inherited visitor rejection / stop behavior, visible-entry filtering, dirent identity emission from durable entry-set location, and typed benign-versus-critical handling for unrecognized directory entries on the enumeration-facing surface. User-carried forward cleanup also absorbs the unresolved lookup-side `iocharset` seam from `pass_01`: either the published codec policy shapes a real lookup/enumeration codec boundary here, or the seam is narrowed / documented precisely enough that it no longer survives as an inert no-op publication consumer. | Enumeration has a distinct public behavior boundary: offset/progress handling, visitor failure propagation, visible-entry filtering, and unrecognized-entry presentation. Keeping it separate lets `pass_01` stabilize child resolution while `pass_02` explicitly proves enumeration reuses the same durable DirectoryEntrySet and Up-case Table / identity truths rather than inventing a parallel namespace path. The user-directed carry-forward avoids reopening accepted `pass_01` solely for the inert codec seam. | Planned; absorb the carried `iocharset` seam cleanup before or with `pass_02` dispatch. |
| `pass_03_directory_lookup_and_identity_integration` | Meso Integration Checker Pass | Cross-surface validation that inherited lookup and enumeration surfaces present one coherent namespace view over the same Up-case Table On-disk Structure Owner and DirectoryEntrySet truth: mixed-case and trailing-dot-sensitive lookup, alias-equivalent identity reuse, repeated-call stability, stale-negative revalidation, and integrity-failure separation for fractured / unrecognized entry sets. | The Designer ktest couples lookup identity, enumeration visibility, and anomaly classification across both inherited VFS surfaces, so the final acceptance proof must remain Checker-owned and separate from the two Creator-synced passes. | Planned; dispatch only after `pass_01` and `pass_02` are accepted. |
| `pass_04_directory_lookup_and_identity_lock_boundary_cleanup_01` | Creator Cleanup Pass. Covered micro-features: `Directory-entry sets must be read as contiguous checksum-validated units`; `Stable inode identity comes from directory-entry location`; `Create-oriented resolution cannot trust stale negative-cache state`; `Name matching is encoding-aware, case-folded, and trailing-dot-sensitive`; `Unrecognized directory entries impose typed lookup-facing boundaries` | This post-acceptance cleanup lane repairs the remaining read-side lock-boundary mismatch between the accepted `meso_03` Designer contract and the current implementation. It may narrow or reorganize owner-local directory read helpers so `lookup()` / `readdir_at()` stop relying on transient lockless directory-stream facts across blocking I/O, revalidate facts when required by the accepted Designer contract, and remain disjoint from the regular-file stream-extent lock contract owned by later file-content meso work. | Opened by user priority shift; archive the Creator packet now and hold dispatch sequencing separate from the paused `meso_05` wave. |

**Deferred / Exit Notes:**

- `pass_01` intentionally carries the lookup-facing identity and stale-negative obligations together with Up-case Table consumption and DirectoryEntrySet validation; do not peel alias reuse or create-oriented revalidation into a third early Creator pass unless a future Reviewer rejection proves the current slice is too broad.
- `pass_02` must reuse the durable naming / identity rules already established by `pass_01`; it must not invent a separate enumeration-only identity path, weaker DirectoryEntrySet validation, or weaker unrecognized-entry policy.
- Mentions of inherited VFS names such as lookup, enumeration, or visitor behavior are routing anchors only. Creator packets must not treat this slicing ledger as authorization to invent new aggregate request / outcome carriers, new dispatcher enums, or exact function signatures beyond pre-existing stable kernel interfaces.
- Structural cleanup is not pre-sliced. If Reviewer later finds helper-family, identity-cache, return-carrier, or test-topology debt, open a named cleanup pass with explicit objectives instead of widening `pass_01` or `pass_02` after the fact.
- `pass_04_directory_lookup_and_identity_lock_boundary_cleanup_01` is a contract-alignment cleanup only. It may repair read-side lock/revalidation behavior and adjacent owner-local helper structure, but it must not absorb regular-file mapping locks or reopen directory semantics that belong to accepted `meso_03` behavior passes except where needed to preserve the same externally visible results under the corrected inlet rules.

### `meso_04_directory_entry_mutation`

**Decision:** Architect and Designer artifacts are accepted for the baseline `meso_04` surface, and the follow-up architecture repair for single-entry / published-entry-set field mutation is accepted. This meso now includes one planned post-integration substrate pass for validated field republish and slot-aligned cleanup support, plus one new post-acceptance lock-boundary cleanup pass, in addition to the earlier owner-local skeleton and user-visible create / remove / rename passes.

| Candidate Pass Area | Current Decision | Rationale |
| :--- | :--- | :--- |
| `pass_01_directory_entry_mutation_direntry_owner_local_skeleton` | Creator Cleanup Pass. Covered micro-features: `File and directory names live in consecutive directory-entry sets guarded by set checksums`; `directory removal and directory-target rename require an emptiness gate`; `Unrecognized directory entries impose typed invalidity and no-modify boundaries instead of generic ignore behavior` | `meso_03` established low-level read-side helpers, but it did not leave behind one shared owner-local directory-entry mutation substrate. Without an explicit first pass, `create`, `unlink` / `rmdir`, and `rename` would each re-open scan / classification / emptiness / slot-location logic inside long `inode.rs` methods. This pass may introduce only an owner-local internal seam for validated entry-set walking, slot-range / vacancy discovery, emptiness scanning, and typed invalidity classification under `ExfatInode(dir)`; it must not invent a new public facade, request / outcome carrier family, or Designer-frozen generic interface layer. |
| `pass_02_directory_entry_mutation_create_mkdir_publication` | Creator-Synced Pass. Covered micro-features: `File and directory names live in consecutive directory-entry sets guarded by set checksums`; `create and mkdir secure directory slots before committing new entry sets`; `zero_size_dir changes only the newborn directory's initial allocation shape`; `Asterinas path-tree surface is positional and still requires explicit refusal boundaries` | After `pass_01` establishes the shared owner-local direntry substrate, creation becomes the first coherent user-visible publication boundary: parent slot reservation, optional parent growth, contiguous checksum-valid entry-set publication, and newborn-directory shape rules can be implemented without entangling delete or rename ordering. This pass should also close the local refusal boundary for unsupported adjacent tree-mutation surfaces that are naturally exercised near the inherited create-side routing. |
| `pass_03_directory_entry_mutation_unlink_rmdir_ordering` | Creator-Synced Pass. Covered micro-features: `unlink and rmdir invalidate entry sets before freeing their cluster state`; `directory removal and directory-target rename require an emptiness gate`; `Unrecognized directory entries impose typed invalidity and no-modify boundaries instead of generic ignore behavior` | Delete-like paths share one distinct correctness contract: namespace-first invalidation, live emptiness gating for directory removal, typed refusal on invalid / unrecognized target material, and allocator handoff that never leaves a visible name pointing at reclaimed clusters. Keeping them together gives Checker one clear failure-ordering proof. |
| `pass_04_directory_entry_mutation_rename_ordering` | Creator-Synced Pass. Covered micro-features: `Cross-directory rename secures the new home before invalidating the old one`; `rename accepts ordinary semantics and RENAME_NOREPLACE only`; `Cross-directory rename crash windows may transiently duplicate reachability rather than orphan it`; `directory removal and directory-target rename require an emptiness gate`; `Asterinas path-tree surface is positional and still requires explicit refusal boundaries` | Rename is the highest-coupling mutation path: it spans same-directory and cross-directory rewriting, destination-first ordering, durable-identity lock order, the allowed rename semantic boundary, and directory-target emptiness. It should not be mixed with creation or delete-like cleanup because those would blur the crash-window and multi-directory admission proof. |
| `pass_05_directory_entry_mutation_integration` | Meso Integration Checker Pass | Integration remains Checker-owned and waits for accepted implementation passes. It should validate the full create → cross-directory rename → unlink / rmdir namespace sequence plus failure-maintenance scenarios after `pass_02` through `pass_04` are closed. |
| `pass_06_directory_entry_mutation_entry_set_field_update_substrate` | Creator-Synced Pass. Covered micro-features: `Validated published file-entry-set field updates must republish one checksum-valid set`; `File and directory names live in consecutive directory-entry sets guarded by set checksums`; `unlink and rmdir invalidate entry sets before freeing their cluster state`; `Unrecognized directory entries impose typed invalidity and no-modify boundaries instead of generic ignore behavior` | This follow-up pass owns the reusable write-side substrate that later metadata meso components need: admit one already-validated published file entry set as mutable material, share the existing decode / validation invariants with the immutable read path instead of cloning a second parser, republish recognized-field byte updates with a refreshed checksum, and keep slot-aligned invalidation / publication cleanup available as a narrower writable-span path that does not itself prove ordinary mutability. Keeping it separate avoids retroactively widening the earlier user-visible namespace passes while still closing the capability gap those later metadata waves would otherwise have to reopen themselves. |
| `pass_07_directory_entry_mutation_lock_boundary_cleanup_01` | Creator Cleanup Pass. Covered micro-features: `File and directory names live in consecutive directory-entry sets guarded by set checksums`; `create and mkdir secure directory slots before committing new entry sets`; `unlink and rmdir invalidate entry sets before freeing their cluster state`; `Cross-directory rename secures the new home before invalidating the old one`; `rename accepts ordinary semantics and RENAME_NOREPLACE only`; `Cross-directory rename crash windows may transiently duplicate reachability rather than orphan it`; `directory removal and directory-target rename require an emptiness gate`; `Asterinas path-tree surface is positional and still requires explicit refusal boundaries`; `Unrecognized directory entries impose typed invalidity and no-modify boundaries instead of generic ignore behavior` | This post-acceptance cleanup lane repairs the remaining mutation-side lock-boundary mismatch between the accepted `meso_04` Designer contract and the current implementation. It should align the mutating inlet and shared serialization path across `create`, `unlink`, `rmdir`, and `rename`, close the uncovered `unlink()` admission path, preserve durable-identity ordering for cross-directory rename, and keep allocator entry subordinate to already-settled namespace admission / emptiness / collision decisions without widening the meso into new semantics. |

**Deferred / Exit Notes:**

- `pass_01` is not a license to invent a reusable public `direntry` API. The intended target is an owner-local internal substrate under `ExfatInode(dir)` and adjacent exFAT-local code only.
- `pass_01` may create a dedicated `direntry`-focused Rust file only if the Creator can justify that the seam is still owner-local / exFAT-local and materially reduces duplicated scan logic without introducing a new carrier family.
- Later passes must reuse the `pass_01` substrate instead of cloning directory-entry walking, emptiness scanning, or typed invalidity classification loops into separate long methods.
- `pass_02` through `pass_04` remain user-visible behavior passes. If `pass_01` grows beyond the internal direntry skeleton boundary, reject it and route a narrower repair instead of silently absorbing public mutation semantics early.
- `pass_07_directory_entry_mutation_lock_boundary_cleanup_01` is a lock-contract alignment cleanup only. It may tighten the shared mutating inlet, close uncovered mutation entry points, and narrow ad hoc serialization seams, but it must preserve the already accepted namespace ordering and refusal semantics rather than re-slicing create / delete / rename behavior into a new public contract.
- `pass_06` must preserve the distinction between two write-side roles: validated published entry-set field republish versus writable slot-span cleanup / invalidation. The latter is a narrower byte-authoring capability and must not become proof that arbitrary directory bytes are ordinarily mutable.
- `pass_06` should maximize code sharing with the existing read-side validation path. If the implementation starts cloning file-entry-set parse / checksum / recognized-field classification logic into a parallel mutable stack, reject it and reopen the design rather than accepting two near-duplicate direntry pipelines.

### `meso_05_file_content_mapping_and_cached_io`

**Decision:** Architect and Designer artifacts are accepted. `pass_01` through `pass_04` remain accepted / closed as the baseline read-side implementation lanes, but the accepted `lock_structure_repair_01` wave now owns any further shared lock-substrate implementation before the reserved `pass_05` integration lane may reopen.

| Candidate Pass Area | Current Decision | Rationale |
| :--- | :--- | :--- |
| `pass_01_file_content_mapping_and_cached_io_shared_read_path_and_lock_boundary` | Creator-Synced Pass. Covered micro-features: `Ordinary cached I/O uses generic cached-data paths with exFAT-specific boundary guards`; `Block mapping and size mutation require a synchronization boundary against truncate` | This first lane is user-fixed. It closes the shared read-path factoring and lock-boundary repair needed before any user-visible regular-file mapping surface can safely build on it. The pass may touch owner-local shared stream/backing translation code and the currently adjacent directory read helper / lock choreography only as a prerequisite repair to avoid duplicating `NoFatChain` / FAT-chain traversal and to remove the current directory/file lock inconsistency. It must not widen into new directory semantics, namespace behavior, or regular-file mutation publication. |
| `pass_02_file_content_mapping_and_cached_io_byte_read_and_initialized_boundary` | Creator-Synced Pass. Covered micro-features: `Stream Extension encodes allocation topology and initialized-data extent`; `Ordinary cached I/O uses generic cached-data paths with exFAT-specific boundary guards` | Once the shared read-side substrate and lock boundary are stable, byte-read behavior is the narrowest user-visible file surface: EOF clipping, `ValidDataLength` zero-observable suffix handling, wrong-kind refusal, invalid-range rejection, and imported read-side fast-fail posture. Keeping it separate lets Checker prove initialized-data exposure without yet coupling in `bmap` or page-cache backend async segmentation. |
| `pass_03_file_content_mapping_and_cached_io_block_mapping_and_page_count` | Creator-Synced Pass. Covered micro-features: `Block mapping takes an O(1) path for contiguous NoFatChain streams`; `Stream Extension encodes allocation topology and initialized-data extent`; `Ordinary cached I/O uses generic cached-data paths with exFAT-specific boundary guards` | Logical block mapping and `npages` are one coherent read-side translation lane: both consume the shared stream-shape truth, both must respect EOF / initialized-data boundaries, and both are narrower than full page-cache backend BIO translation. Keeping them together gives one focused proof that contiguous arithmetic mapping and FAT-chain traversal agree with file length / page-count reporting. |
| `pass_04_file_content_mapping_and_cached_io_page_cache_backend_translation` | Creator-Synced Pass. Covered micro-features: `PageCacheBackend must translate page indices into possibly fragmented disk I/O without blocking inline`; `Stream Extension encodes allocation topology and initialized-data extent`; `Ordinary cached I/O uses generic cached-data paths with exFAT-specific boundary guards`; `Block mapping and size mutation require a synchronization boundary against truncate` | Backend page translation is the highest-coupling read-side path: it depends on the shared lock boundary, fragmented backing segmentation, initialized-data zero handling, waiter handoff, and non-blocking rules under page-cache call sites. It is therefore intentionally last among implementation passes, after the lower-level byte-read and block-mapping surfaces have stabilized. |
| `pass_05_file_content_mapping_and_cached_io_integration` | Meso Integration Checker Pass (reserved / blocked behind `lock_structure_repair_01`) | Integration remains Checker-owned, but it must stay closed until the routed `lock_structure_repair_01` passes re-establish topology conformance for the surviving read-side implementation. Only then should it validate agreement across `read_at`, logical block mapping, `npages`, cached page read translation, cached dirty-page writeback translation, repeated-call stability, failure-maintenance, and the shared truncate anti-race boundary. |

### `lock_structure_repair_01`

**Decision:** Execute the accepted cross-wave lock-repair plan through four routed implementation passes: one shared substrate setup pass plus three parent-meso conformance cleanup passes, followed by one user-directed wave-level integration Checker lane and one user-directed wave-level architectural Reviewer lane. Because Creator / Checker / Reviewer packets still require exactly one parent meso-component, `lock_structure_repair_01` remains the canonical wave name while routed implementation / validation packets bookkeep under `meso_03_directory_lookup_and_identity`, `meso_04_directory_entry_mutation`, and `meso_05_file_content_mapping_and_cached_io`.

| Candidate Pass Area | Current Decision | Rationale |
| :--- | :--- | :--- |
| `pass_01_lock_structure_repair_01_shared_substrate` | Creator Cleanup Pass. Parent meso routing: `meso_05_file_content_mapping_and_cached_io`. Covered boundary: shared live Level-1 admission substrate, true Level-2 owner-local inode admission substrate, narrowed Level-3 stream-shape responsibility, allocator-last ordering hooks, durable-identity-based multi-inode ordering substrate, and the narrow release / revalidate snapshot handoff needed so later blocking storage submission does not depend on a live spin guard. It must not claim final directory-read, directory-mutation, or regular-file mapping conformance by itself. | Isolate the highest-risk shared lock-shape edits before re-homing public entry surfaces. Route it through `meso_05` because the reopened blocker came from `meso_05/pass_04`, the recovered Architect audit was sourced from the `meso_05` sidecar, and later regular-file mapping / mutation / sync work all import the same substrate. |
| `pass_02_lock_structure_repair_01_directory_lookup_rehome` | Creator Cleanup Pass. Parent meso routing: `meso_03_directory_lookup_and_identity`. Covered boundary: `lookup()` / `readdir_at()` and shared directory-read choreography must enter through `ExfatFs state rwlock(Read)` plus directory `ExfatInode rwlock(Read)`, use release / reacquire / revalidate around blocking directory-byte reads, and stop using Level 3 as the owner-local directory-read substitute. | Directory read-side work is the narrowest consumer of the repaired substrate and validates the sleep-boundary choreography before write-side namespace mutation is retargeted. |
| `pass_03_lock_structure_repair_01_directory_mutation_rehome` | Creator Cleanup Pass. Parent meso routing: `meso_04_directory_entry_mutation`. Covered boundary: `create()`, `unlink()`, `rmdir()`, `rename()`, and their shared mutation substrate must use live Level-1 mutation admission, ordered participating directory Level-2 admission, optional Level-3 stream-shape entry only when needed, allocator-last ordering, and removal of `directory_mutation` as the structural authority. It must close lower-to-higher acquisition inversions without reopening accepted namespace or rename semantics. | This is the highest-coupling directory-side repair and should wait until the shared substrate and read-side directory admission have stabilized. |
| `pass_03_lock_structure_repair_01_directory_mutation_rehome_cleanup_01` | Creator Cleanup Pass. Parent meso routing: `meso_04_directory_entry_mutation`. Covered boundary: keep the accepted `pass_03` directory-mutation rehome shape, but close the residual Level-2 admission debt named by the wave-level architectural Reviewer: `rmdir()` and both `rename()` branches must acquire the ordered participating directory write set before source/target resolution, directory-target emptiness must admit the child directory through Level 2, and the surviving child-directory special-case stream-snapshot seam must disappear. | `pass_06_lock_structure_repair_01_architectural_reviewer` rejected for three specific residuals confined to the meso_04 mutation surface. Route them back into a narrow Creator cleanup pass instead of reopening the repaired regular-file or test-topology surfaces. |
| `pass_04_lock_structure_repair_01_regular_file_mapping_rehome` | Creator Cleanup Pass. Parent meso routing: `meso_05_file_content_mapping_and_cached_io`. Covered boundary: regular-file `read_at()`, block mapping, `npages()`, `read_page_async()`, `write_page_async()`, and adjacent cached-I/O translation must move onto Level-2 file-owner admission plus Level-3 stream-shape exclusion, with mapping snapshots and submission tokens surviving after the narrow lock window ends so no spin-based guard crosses block-device read, BIO submission, or wait boundaries. | This closes the surviving `meso_05` structural mismatch only after the shared substrate and directory-side conformance are in place, leaving the future `meso_06` / `meso_07` lock story compatible with the frozen topology. |
| `pass_05_lock_structure_repair_01_integration_checker` | Meso Integration Checker Pass. User-directed wave gate routed under `meso_05_file_content_mapping_and_cached_io`. Covered boundary: run only the related integration ktests for the completed `lock_structure_repair_01` wave — rerun the existing `meso_03` and `meso_04` integration exact-name ktests, and implement/run the missing `meso_05` integration exact-name ktests for read / mapping / page-cache coherence, failure-maintenance, repeated-call stability, and truncate-boundary serialization. Do not run unit ktests here. | The user explicitly replaced the normal per-pass runtime flow with one post-wave integration Checker lane. Routing bookkeeping under `meso_05` keeps the execution receipts grouped with the final read-side parent meso while still validating the full wave's affected directory and regular-file integration surfaces. |
| `pass_05_lock_structure_repair_01_integration_checker_rerun_01` | Meso Integration Checker Pass. User-directed wave gate rerun routed under `meso_05_file_content_mapping_and_cached_io`. Covered boundary: rerun the exact same integration-ktest surface as `pass_05_lock_structure_repair_01_integration_checker` after `pass_03_lock_structure_repair_01_directory_mutation_rehome_cleanup_01` lands, because the wave-level runtime evidence becomes stale once the directory-mutation production surface changes again. Do not add unit ktests here. | The reviewer rejection is structural rather than test-topology-only, so the accepted `pass_05` receipts no longer prove the post-cleanup state. The wave must refresh its single user-directed runtime gate before Reviewer follow-up. |
| `pass_06_lock_structure_repair_01_architectural_reviewer` | Reviewer Pass. User-directed wave-level architectural gate routed under `meso_05_file_content_mapping_and_cached_io` for bookkeeping while artifacts remain under `lock_structure_repair_01`. Covered boundary: judge whether the surviving global lock structure now conforms to `macro_00_global_topology.md` plus the accepted `lock_structure_repair_01` architect/designer anchors after the wave-level integration Checker passes, and re-audit the packet-touched test-support surface from `pass_05`. | This is the user-directed final structural gate for the lock-repair wave before any focused `meso_06` / `meso_07` Designer repair packet may open. |
| `pass_06_lock_structure_repair_01_architectural_reviewer_followup` | Reviewer Pass. User-directed wave-level architectural follow-up gate routed under `meso_05_file_content_mapping_and_cached_io` for bookkeeping while artifacts remain under `lock_structure_repair_01`. Covered boundary: re-judge the same global lock-structure and touched test-support surface after `pass_03_lock_structure_repair_01_directory_mutation_rehome_cleanup_01` and `pass_05_lock_structure_repair_01_integration_checker_rerun_01` land. | `pass_06_lock_structure_repair_01_architectural_reviewer` rejected the pre-cleanup state for three residual meso_04 admission defects. The same final gate must re-run after the cleanup and runtime rerun close those residuals. |

**Deferred / Exit Notes:**

- User-directed wave-local execution override for `lock_structure_repair_01`: every routed pass must dispatch a Creator, but no per-pass Reviewer packet or per-pass runtime Checker packet should be scheduled.
- Per-pass validation for this wave is limited to at most one small `cargo check` preflight after the Creator lands; the main agent may run that preflight directly via `.agents/tools/checker_run.sh cargo-check --component <parent-meso> --phase <pass-id>` or skip it when no compile-affecting delta lands, so a dedicated Checker packet is optional rather than mandatory.
- After all routed `lock_structure_repair_01` passes close, schedule exactly one Checker-owned integration lane covering only the related integration ktests for this wave; do not schedule unit ktests for the intermediate passes or the final integration gate.
- After that integration lane passes, schedule exactly one architectural Reviewer lane to judge whether the surviving global lock structure now conforms to the accepted `macro_00` topology and the `lock_structure_repair_01` architect/designer anchors.
- If that final architectural Reviewer rejects for structural cleanup and the route returns to Creator, rerun the same single wave-level integration gate before the Reviewer follow-up; the previously accepted runtime receipts are stale once production lock structure changes again.
- Do not open a standalone cross-wave Creator packet that lacks a parent meso-component; route packets through the parent mesos listed above while keeping the `lock_structure_repair_01` wave name in the pass IDs and packet / artifact naming.
- Do not reopen accepted Designer artifacts for `meso_03_directory_lookup_and_identity`, `meso_04_directory_entry_mutation`, or `meso_05_file_content_mapping_and_cached_io`; these routed passes are substrate-conformance work only.
- After `pass_05_lock_structure_repair_01_integration_checker` plus any required rerun and `pass_06_lock_structure_repair_01_architectural_reviewer` plus any required follow-up both pass, dispatch the focused `meso_06_file_content_mutation` Designer repair first, then the focused `meso_07_file_sync_and_persistence` Designer repair.
- Keep `meso_05_file_content_mapping_and_cached_io` integration reserved and keep all `meso_06` / `meso_07` Creator slicing closed until the routed `lock_structure_repair_01` passes and the later two Designer repair packets are accepted.

### `meso_06_file_content_mutation`

**Decision:** The focused post-`lock_structure_repair_01` Designer repair is accepted, `pass_01_file_content_mutation_internal_helper_boundary_and_backend_dereentry` is accepted/closed as the seam substrate, and the later `meso_06` work is now intentionally sliced into two additional Creator passes plus one later meso integration pass. Do not over-split the remaining visible mutation work: keep the public write/refusal boundary separate from the topology-bearing growth/shrink pass, then reserve integration for after both implementation passes stabilize.

| Pass ID | Kind | Covered Boundary | Slicing Rationale | Decision State |
| :--- | :--- | :--- | :--- | :--- |
| `pass_01_file_content_mutation_internal_helper_boundary_and_backend_dereentry` | Creator-Synced Pass | Regular-file owner-internal admitted-state helper seam for the packeted `inode.rs` data path; backend-facing `page_cache` read / write / `npages` / initialization routing that must consume published facts plus already chosen file-local state without public-admission reentry; simplification or removal of regular-file helpers, thin forwarding wrappers, and snapshot-as-substitute routing whose only justification was the missing admitted-state seam; closure of the currently known child symptoms of that parent problem inside the regular-file data-path scope. This pass does **not** yet claim final user-visible write / append / resize publication, allocation growth, or sync-intent completion semantics. | Partial-page cached writes can force cache refill from inside an outer mutation path. If visible mutation work lands before this seam is repaired, the implementation either deadlocks or preserves the same architecture defect by bypassing generic `page_cache`, forbidding refill shapes, or carrying snapshot/workaround debt forward. The first pass must therefore repair the parent structure directly and collapse the known symptom surface instead of routing around it. | Accepted / closed through Creator, user-directed Creator repair 01, synchronized Checker, narrow Checker rerun 01, and Reviewer. |
| `pass_02_file_content_mutation_write_boundary_and_refusal_paths` | Creator-Synced Pass | Public regular-file `write_at` / append-intent mutation inlet on top of the accepted pass01 seam, including write-side zero-fill and initialized-data publication **when no new allocation/free topology needs to be introduced**, content-change timestamp triggering for those successful writes, `O_DIRECT` alignment rejection before side effects, explicit fallocate-family refusal, and recording/routing of `O_SYNC` / `O_DSYNC` persistence intent without absorbing `meso_07` file-sync semantics. Keep this pass out of EOF-extending allocation topology, shrink/free, `NoFatChain` transition, and orphan-vs-visible append failure ordering. | After pass01, the next safest slice is to open the public mutation boundary and the refusal/flag surfaces without simultaneously landing allocator/FAT topology rewrites. This gives later growth/shrink work a stable public inlet and keeps direct-I/O / fallocate / sync-intent boundary code from being buried inside the topology-heavy pass. | Accepted / closed through Creator, synchronized Checker, Reviewer, and two ultra-small main-agent production micro-fixes during the Checker lane. |
| `pass_03_file_content_mutation_growth_shrink_and_allocation_topology` | Creator-Synced Pass | The remaining topology-bearing visible mutation work: EOF-extending writes and append publication, resize growth and shrink, Stream Extension truth maintenance for `DataLength` / `ValidDataLength` / `FirstCluster` / `NoFatChain`, allocator/Allocation Bitmap/FAT coordination, contiguous-to-fragmented transition with FAT backfill, chain shortening and cluster release, append crash-window ordering that prefers orphanable allocation over exposed uninitialized EOF, and the length-side timestamp/publication consequences of those mutations. This pass also owns the remaining zero-fill and truncate anti-race aspects that require real size/topology change rather than the boundary-only write path from pass02. | Growth, shrink, `NoFatChain` transition, allocator/FAT publication, and append failure-maintenance form one indivisible stream/allocator story. Splitting them further would create artificial half-topology passes with ambiguous Checker obligations and would re-open the same publication-order risks across multiple routes. | Accepted / closed through Creator, synchronized Checker, and Reviewer; final Checker skippable per Reviewer verdict. |
| `pass_04_file_content_mutation_integration` | Meso Integration Pass | Meso-level success/failure/repeated-call/concurrency scenarios after pass02 and pass03 are both accepted: ordinary writes, append-intent writes, gap writes, non-contiguous growth, shrink, readback-visible zero-fill, publication/order maintenance, and shared stream/allocator invariants under concurrent read/mapping observation. Keep explicit fallocate refusal and direct-I/O alignment as Creator-synced/unit-checker proof surfaces unless later implementation evidence shows they need extra meso coupling. | The Designer contract’s integration story only becomes meaningful after the public mutation boundary and the topology-bearing mutation pass both exist. Keep integration last so the Checker validates one coherent visible mutation surface rather than replaying unstable partial substrates. | Accepted / closed through the dedicated Checker integration lane with `make-kernel` plus four exact-name meso integration ktests and archived serial-log proof. |

**Deferred / Exit Notes:**

- `pass_01_file_content_mutation_internal_helper_boundary_and_backend_dereentry` is required to solve the parent helper-layer gap directly. Do not reinterpret the pass as “implement writes first but avoid generic `page_cache`.”
- The pass boundary explicitly includes cleanup of known regular-file helper/snapshot symptoms caused by the missing internal seam, but it does **not** reopen already accepted directory-side `meso_03` / `meso_04` routing work or the accepted `lock_structure_repair_01` wave.
- `pass_02_file_content_mutation_write_boundary_and_refusal_paths` is intentionally the next pass. Do not drag allocator/FAT topology or shrink/free into it merely because the public write boundary becomes available there.
- `pass_03_file_content_mutation_growth_shrink_and_allocation_topology` is intentionally broad. Do not peel append growth, resize growth, shrink/free, and `NoFatChain` / FAT-backfill work into separate micro-passes unless later evidence shows the combined topology story is still too large to stabilize.
- Meso integration validation is now closed for `meso_06_file_content_mutation`.

### `meso_07_file_sync_and_persistence`

**Decision:** The focused post-`lock_structure_repair_01` Designer repair is accepted, and `meso_07` is now intentionally sliced into two Creator-synced implementation passes plus one later meso integration pass. Do not open a separate pre-wave structural cleanup lane first: if file-local dirty-window bookkeeping needs a new owner-local seam, keep that work inside the second pass rather than reopening `meso_06` or the lock-repair wave.

| Pass ID | Kind | Covered Boundary | Slicing Rationale | Decision State |
| :--- | :--- | :--- | :--- | :--- |
| `pass_01_file_sync_and_persistence_writeback_ordering_and_admission_boundary` | Creator-Synced Pass | The first real ordinary-file `sync_all()` / `sync_data()` inlet on the repaired Level-1 plus Level-2 substrate: pre-admission fast-fail on imported anomaly posture, ordered composition of admitted file writeback before backing-device sync and final device-cache flush, and the base success boundary for `sync_data` versus `sync_all` without yet claiming the full post-wakeup revalidation or repeated-call story. This pass must keep persistence file-scoped and must not absorb stream mutation, allocator ownership, or mount-wide `FileSystem::sync()` policy. | The current production seam is still an `Ok(())` stub, so the safest first lane is to make the inherited ordinary-file sync anchors real while staying anchored on the already accepted lock substrate and `meso_06` publication handoff. Landing that base ordering first prevents later wait-boundary or metadata-scope work from being buried inside a zero-to-everything rewrite. | Accepted / closed through Creator, main-agent local writeback-tail micro-fix, Checker rerun, and Reviewer. |
| `pass_02_file_sync_and_persistence_revalidation_metadata_scope_and_failure_maintenance` | Creator-Synced Pass | The remaining full correctness story after the base sync seam exists: reacquire Level 1 then Level 2 after page-cache writeback or block-device waits, revalidate forced-shutdown / fast-fail posture, preserve the admitted dirty window across writeback or device-stage failure, guarantee that no spin-based guard survives writeback submission, BIO waits, device sync, or flush completion, and close the remaining `sync_data` versus `sync_all` metadata boundary, metadata-only dirty intervals, repeated clean-call stability, and success publication that clears only the admitted window while leaving later re-dirty work outstanding. If a new owner-local dirty-window ledger or dirty-class seam is required, it belongs here and must stay owner-local rather than becoming a pre-wave cross-component cleanup. | The remaining wait-boundary, failure-maintenance, and metadata-scope work all revolve around one shared question: how one admitted dirty window is tracked, revalidated, and cleared without falsely claiming the whole file clean. Splitting those surfaces further would likely force the same owner-local dirty-state seam to be introduced in one pass and then reopened immediately in the next. | Accepted / closed through Creator repair 01, Checker rerun 01, and Reviewer follow-up 01; final Checker is skippable because Reviewer made no `.rs` edits. |
| `pass_03_file_sync_and_persistence_integration` | Meso Integration Pass | Meso-level success/failure/repeated-call/concurrency scenarios after pass01 and pass02 are both accepted: `sync_data()` then `sync_all()` ordering on the same file, post-wait anomaly or device failure maintenance, metadata-only dirty-interval behavior, and concurrent re-dirty or forced-shutdown races while one sync request sleeps. Keep mount-wide sync ownership with `meso_08` and mutation/allocation ownership with `meso_06`. | The Designer contract’s integration story only becomes meaningful once both the base ordering seam and the combined revalidation/metadata-scope truth exist together. Keep integration last so the Checker validates one coherent visible file-sync surface rather than replaying unstable partial seams. | Accepted / closed through the dedicated Checker integration lane with `make-kernel` plus four exact-name meso integration ktests and archived serial-log proof. |

**Deferred / Exit Notes:**

- Do not reopen `lock_structure_repair_01`, `meso_05`, or `meso_06` before starting `meso_07`; the accepted Level-1 plus Level-2 substrate and the released sync-intent handoff are the frozen base assumptions for these passes.
- If `pass_02` needs new owner-local dirty-state bookkeeping to separate `sync_data` from `sync_all`, keep it confined to `ExfatInode` / `inode.rs`; do not spin it out into a standalone cleanup wave or a new cross-owner carrier family.
- Keep mount-wide anomaly clearing, `VolumeDirty` rewriting, and filesystem-wide quiesce out of `meso_07`; those remain reserved for `meso_08_filesystem_sync_and_volume_state`.

### `meso_08_filesystem_sync_and_volume_state`

**Decision:** Architect and Designer artifacts are accepted. Implementation is sliced into three Creator-synced passes plus one independent meso integration Checker pass.

| Pass ID | Kind | Covered Boundary | Slicing Rationale | Decision State |
| :--- | :--- | :--- | :--- | :--- |
| `pass_01_filesystem_sync_and_volume_state_posture_and_admission_boundary` | Creator-Synced Pass | Mount-wide observation / mutation-admission posture under `ExfatFs`: imported `VolumeDirty`, `MediaFailure`, `ClearToZero`, read-only posture for state-changing mount-wide admission, and a coherent owner-local boundary for sibling mutators / discard-adjacent consumers. This pass must preserve anomaly visibility, must not clear or rewrite `VolumeDirty`, must not claim durable clean publication, and must not add a speculative public forced-shutdown ABI. If no accepted public forced-shutdown hook exists, Checker records that public-route coverage as not applicable rather than requiring a synthetic `EOPNOTSUPP` surface. | The current code has `admitted_lookup_state()` / `admitted_mutation_state()` and direct imported anomaly snapshots, but no coherent meso08 mount-wide posture seam. Landing this first prevents `FileSystem::sync()` from burying admission policy inside helper code and gives later dirty-bracket and shutdown work one owner-local state source. | Accepted / closed through Creator, Checker, and Reviewer. Final Checker is skippable because Reviewer made no `.rs` edits. |
| `pass_02_filesystem_sync_and_volume_state_sync_dirty_bracket_and_failure_maintenance` | Creator-Synced Pass | Whole-filesystem `FileSystem::sync()` plus the `VolumeDirty` durability bracket: state-changing admission marks or preserves dirty posture before lower-level mutation can leave mount-wide work outstanding, sync establishes the quiesce point, persists the clean/dirty volume-state transition only at the correct durability point, issues the final device barrier, revalidates after blocking I/O, and leaves dirty / degraded posture retryable on persistence or flush failure. This pass must not silently clear `MediaFailure` or `ClearToZero`, must not treat one inode-local `fsync` as whole-filesystem quiesce, and must not absorb forced-shutdown monotonicity. | `VolumeDirty` and recommended write ordering are the central durable-state risk. They need a synchronized Checker pass after the pass01 posture seam exists, but before forced-shutdown cross-cuts the same admission surfaces. Explicitly including the dirty-entry boundary avoids leaving integration tests with no way to create mount-wide outstanding persistence work. | Accepted / closed through Creator, Checker, Reviewer rejection, cleanup Creator, cleanup Checker, and cleanup Reviewer. Final Checker is skippable because cleanup Reviewer made no `.rs` edits. |
| `pass_03_filesystem_sync_and_volume_state_forced_shutdown_and_suppression` | Creator-Synced Pass | Forced-shutdown admission through an accepted internal route only, monotonic shutdown posture for the lifetime of the mount, and downstream suppression for ordinary mutation / discard-adjacent consumers. Public forced-shutdown routing remains unsupported unless a later accepted routing surface exists; this pass must not invent public admin ABI and must not let later sync / observation reopen writable posture. | Forced shutdown is cross-cutting and can distort the dirty-bracket proof if mixed into pass02. Keeping it separate lets Creator, Checker, and Reviewer focus on monotonicity, caller suppression, and no speculative public API growth. | Accepted / closed through Creator, Checker, and Reviewer. Final Checker is skippable because Reviewer made no `.rs` edits. |
| `pass_04_filesystem_sync_and_volume_state_integration` | Meso Integration Pass | End-to-end Designer Section 3 scenarios after pass01-pass03 acceptance: lower-level mutation leaves mount-wide persistence work, `FileSystem::sync()` quiesces it while preserving root / superblock state, failure leaves conservative dirty / degraded posture, repeated sync / observation is stable, and sync / observation / shutdown contention linearizes. | Integration is meaningful only after the posture seam, dirty-bracket sync semantics, and shutdown monotonicity all exist. It stays Checker-owned and must not fold new production policy into the integration lane. | Accepted / closed through the dedicated Checker integration lane and post-integration Reviewer. After the test-support file was renamed to `volume_sync_integration.rs`, a narrow rename-verification Checker reran `make-kernel` and the four exact-name integration ktests under the new module path; all passed. |

### `meso_09_file_metadata_projection_and_update`

**Decision:** Architect and Designer artifacts are accepted. Implementation is sliced into two Creator-synced passes plus one independent meso integration Checker pass. Keep this meso scoped to regular-file metadata projection/update under `ExfatInode(file)`; do not absorb directory metadata, file content/size mutation, file sync durability, or volume administrative identity.

| Pass ID | Kind | Covered Boundary | Slicing Rationale | Decision State |
| :--- | :--- | :--- | :--- | :--- |
| `pass_01_file_metadata_projection_and_update_projection_substrate` | Creator-Synced Pass | Read-side regular-file metadata projection through the inherited inode metadata getters: `metadata()`, `mode()`, `owner()`, `group()`, `atime()`, `mtime()`, and `ctime()`. Covered micro-features: `File entry sets persist DOS-style attributes and local-time timestamp triples`; `Ownership and mode are mount-derived rather than natively persisted as POSIX metadata`; `Timestamp translation bridges UTC-facing VFS state to exFAT local-time fields with offset bytes`; `Asterinas metadata surface requires explicit owner, mode, and timestamp answers even when exFAT synthesizes them`. The pass must decode durable DOS attributes and timestamp families, synthesize uid/gid/mode/ctime from mount policy and the thin exFAT model, import sibling file-size / allocated-sector truth only for projection, and perform no durable metadata rewrite. | The read projection is the stable substrate needed by every later mutator and by integration reread checks. Landing it first avoids mixing getter correctness, checksum-valid entry-set republish, and policy-sensitive mutation failure handling in one large pass. | Accepted / closed through Creator, synchronized Checker, Reviewer, and final Checker rerun. |
| `pass_02_file_metadata_projection_and_update_policy_and_timestamp_mutation` | Creator-Synced Pass | Regular-file metadata mutation after the pass01 projection substrate exists: `set_mode()` maps only projected writability to the durable DOS `ReadOnly` attribute and republishes the same validated entry set; `set_owner()` / `set_group()` confirm or refuse the mount-derived ownership envelope without inventing exFAT owner fields; admitted `set_atime()` / `set_mtime()` rewrite only the owned exFAT timestamp family and UTC-offset / 10ms fields; `set_ctime()` updates only the live synthetic metadata-change projection and must not fabricate a native exFAT `ctime`; denied metadata updates preserve projected and durable state. Covered micro-features: all six meso09 micro-features, with mutation emphasis on `File entry sets persist DOS-style attributes and local-time timestamp triples`; `Ownership and mode are mount-derived rather than natively persisted as POSIX metadata`; `chmod only materially propagates the writable bit through the DOS ReadOnly attribute`; `Timestamp translation bridges UTC-facing VFS state to exFAT local-time fields with offset bytes`; `Timestamp mutation is also gated by mount-time allow_utime policy`; `Asterinas metadata surface requires explicit owner, mode, and timestamp answers even when exFAT synthesizes them`. The pass must preserve name/slot/secondary topology, create timestamps on unrelated updates, file size, stream topology, and allocation state; successful durable rewrites publish file-local metadata dirtiness for `meso_07_file_sync_and_persistence`. | Mode/ownership policy and timestamp mutation share the same regular-file metadata update boundary: mount-policy admission, validated entry-set republish, checksum preservation, refusal without partial durable state, and file-local metadata dirty publication. Merging them reduces scheduling overhead while keeping the read projection substrate separate enough to stabilize the later mutation Checker. | Accepted / closed through Creator, synchronized Checker, Reviewer rejection, cleanup Creator, cleanup Checker, and cleanup Reviewer follow-up. Final Checker skipped because the follow-up Reviewer edit was a line-level non-functional production comment only and no tests or Checker-owned surfaces changed. |
| `pass_02_file_metadata_projection_and_update_policy_and_timestamp_mutation_cleanup_01` | Creator Cleanup Pass | Same pass02 covered-micro boundary; structural closure only for Reviewer-rejected helper/census and mount-policy seam objectives: `FileEntrySetView::is_read_only`; `FileEntrySetView::last_accessed_fields`; `FileEntrySetView::last_modified_fields`; unresolved `allow_utime` / timezone mount-policy seam lacking code-level exit-plan comment. | Reviewer accepted the pass02 runtime behavior but rejected structural quality. Cleanup must close the named objectives individually without adding new metadata feature coverage or hiding the seam in generic wording. | Accepted / closed through Creator, Checker, and Reviewer follow-up. Final Checker skipped per protocol because Reviewer made only a comment-only production edit documenting the `set_mtime()` exit plan. |
| `pass_03_file_metadata_projection_and_update_integration` | Meso Integration Pass | End-to-end Designer Section 3 scenarios after pass01-pass02 acceptance: live and reread metadata projection agree after admitted mode/timestamp changes and file sync; failure paths preserve the last good projected and durable metadata; repeated metadata getters / no-op confirmations are stable; controlled races with sibling content mutation or file sync revalidate policy and self-entry-set state before publication. Covered micro-features: all six meso09 micro-features. | Integration only becomes meaningful after read projection and the combined metadata mutation pass both exist. It stays Checker-owned and must not fold new production behavior into the integration lane. | Accepted / closed through Checker, creator_repair_01, Checker rerun, and post-integration Reviewer. Final Checker skipped because Reviewer made no `.rs` edits. |
| `pass_03_file_metadata_projection_and_update_integration_creator_repair_01` | Creator Repair Pass | Narrow repair for the failed pass03 integration concurrency test: ensure regular-file entry-set republish applies stream-extension field updates before recalculating the checksum, preserving fresh reread validity after serialized metadata and content publication. | The failed integration proof found a cross-surface publication bug rather than a test-topology issue. Route the Checker repair batch to Creator without widening pass03 beyond the entry-set republish checksum-ordering defect. | Accepted / closed through Checker rerun and post-integration Reviewer. |

**Deferred / Exit Notes:**

- Do not reopen `meso_05_file_content_mapping_and_cached_io` or `meso_06_file_content_mutation` for size / allocated-sector projection; meso09 imports those truths but does not mutate them.
- Do not route file-scoped persistence into meso09. Successful metadata rewrites only publish file-local metadata dirtiness; durability and final device flush consequences remain with `meso_07_file_sync_and_persistence`.
- Do not add volume label / GUID / administrative identity APIs here; those belong to `meso_11_volume_admin_identity`.
- Structural cleanup is not pre-sliced. If Reviewer rejects helper placement, carrier seams, timestamp helper families, or test-support topology, open a named cleanup pass with each objective individually listed.

### `meso_10_directory_metadata_projection_and_update`

**Decision:** Architect and Designer artifacts are accepted. Implementation is sliced into three Creator-synced passes plus one independent meso integration Checker pass. Keep this meso scoped to directory metadata under `ExfatInode(dir)`: ordinary-directory self metadata, root-directory synthetic projection, direct directory metadata setters, and namespace-mutation-driven directory timestamp refresh. Do not absorb regular-file metadata from `meso_09_file_metadata_projection_and_update`, namespace ordering from `meso_04_directory_entry_mutation`, file content / sync durability from `meso_06_file_content_mutation` or `meso_07_file_sync_and_persistence`, or volume label / GUID administrative identity from `meso_11_volume_admin_identity`.

| Pass ID | Kind | Covered Boundary | Slicing Rationale | Decision State |
| :--- | :--- | :--- | :--- | :--- |
| `pass_01_directory_metadata_projection_and_update_projection_substrate` | Creator-Synced Pass | Read-side directory metadata projection through inherited inode metadata getters: `metadata()`, `mode()`, `owner()`, `group()`, `atime()`, `mtime()`, and `ctime()`. Covered micro-features: `File and directory names live in consecutive directory-entry sets guarded by set checksums`; `File entry sets persist DOS-style attributes and local-time timestamp triples`; `Ownership and mode are mount-derived rather than natively persisted as POSIX metadata`; `chmod only materially propagates the writable bit through the DOS ReadOnly attribute`; `Timestamp translation bridges UTC-facing VFS state to exFAT local-time fields with offset bytes`; `Asterinas metadata surface requires explicit owner, mode, and timestamp answers even when exFAT synthesizes them`; projection-facing coverage of `Refusal paths must preserve typed errno distinctions instead of collapsing into implicit gaps`. The pass must decode durable DOS attributes and timestamp families from one checksum-valid ordinary-directory self entry set, synthesize uid / gid / mode / `ctime` from mount policy and exFAT's durable model, preserve directory type and name-entry topology, return coherent root-directory synthetic metadata without fabricating an ordinary self entry set, and perform no durable metadata rewrite. | Directory read projection is the substrate needed by direct setters, namespace refresh, and reread integration. Landing it first isolates ordinary-directory self entry-set validation and root synthetic projection from checksum-preserving republish and namespace-mutation refresh work. | Accepted / closed through Creator, `creator_repair_01`, Checker rerun, and Reviewer. Final Checker skipped because Reviewer made no `.rs` edits. |
| `pass_02_directory_metadata_projection_and_update_policy_and_timestamp_mutation` | Creator-Synced Pass | Direct directory metadata mutation after pass01 projection exists: `set_mode()` maps only projected writability to DOS `ReadOnly` while preserving the directory attribute bit; `set_owner()` / `set_group()` confirm or refuse the mount-derived ownership envelope without inventing exFAT UID/GID storage; admitted `set_atime()` / `set_mtime()` rewrite only the owned exFAT timestamp families and UTC-offset / 10ms fields; `set_ctime()` remains synthetic or returns a typed refusal and must not fabricate a native exFAT `ctime`; root-directory mutation is refused or bounded by the Designer's synthetic-root policy without pretending a normal self entry set exists. Covered micro-features: all meso10 micro-features, with mutation emphasis on DOS attributes, ownership/mode projection, timestamp translation, `allow_utime` policy, explicit Asterinas metadata answers, and typed refusal distinctions. Successful durable rewrites must republish one checksum-valid ordinary-directory self entry set, update the in-memory metadata view only after durable publication succeeds, and publish file-local metadata dirtiness for `meso_07_file_sync_and_persistence` only as directory-local metadata work. | Direct setters share admission, validated entry-set republish, checksum preservation, root refusal, and typed failure-maintenance boundaries. Keeping them together avoids introducing separate helper seams for mode, ownership, and timestamp updates that would immediately reopen the same self-entry-set rewrite path. The repair loop confirmed this rationale: the original stall was a lock reentry through projection-backed `ino()`, fixed by adding a no-lock middle-helper boundary for ordered directory writes. | Accepted / closed through Creator, `creator_repair_01`, `creator_repair_02`, Checker rerun 02, Reviewer, test-support checksum cleanup Creator, cleanup Checker, and Reviewer follow-up. Final Checker skipped because the follow-up Reviewer made no `.rs` edits and the cleanup Checker already passed after the Checker-owned import-path fix. |
| `pass_02_directory_metadata_projection_and_update_policy_and_timestamp_mutation_test_support_checksum_cleanup_01` | Creator Cleanup Pass | Same pass02 covered-micro boundary; cleanup only for Reviewer-rejected test-support checksum topology: repair `test_support/inode.rs::entry_set_checksum` and remove/collapse duplicate `test_support/directory_metadata_projection_and_update_policy_and_timestamp_mutation.rs::directory_entry_set_checksum`. | The production pass02 surface and Checker runtime proof were accepted, but Reviewer rejected the packeted test-support surface because two checksum helpers encoded the same contract differently. Keep cleanup narrow to the two named test-support files. | Accepted / closed. Creator repaired the shared test-support checksum helper and removed the duplicate pass-local helper; cleanup Checker passed `cargo-check`, `make-kernel`, and all five pass02 exact-name ktests; Reviewer follow-up approved the topology and made no `.rs` edits. |
| `pass_03_directory_metadata_projection_and_update_namespace_refresh` | Creator-Synced Pass | Directory timestamp refreshes triggered by accepted sibling namespace mutation paths: `create`, `mkdir`, `unlink`, `rmdir`, and `rename`. Covered micro-features: `File and directory names live in consecutive directory-entry sets guarded by set checksums`; `File entry sets persist DOS-style attributes and local-time timestamp triples`; `Timestamp translation bridges UTC-facing VFS state to exFAT local-time fields with offset bytes`; `Timestamp mutation is also gated by mount-time allow_utime policy`; `Asterinas metadata surface requires explicit owner, mode, and timestamp answers even when exFAT synthesizes them`; `Refusal paths must preserve typed errno distinctions instead of collapsing into implicit gaps`. The pass must refresh affected source and target directory metadata timestamps after namespace mutation admission, revalidate ordinary-directory self entry-set location under directory write admission before republish, and keep `meso_04_directory_entry_mutation` as owner of namespace reachability, slot reservation, rename ordering, and emptiness gates. | Namespace-driven directory metadata refresh cross-cuts existing meso04 call sites and has a different risk surface from standalone setters: stale slot coordinates, cross-directory handoff, and source/target timestamp coherence. Isolating it prevents pass02 from becoming a broad namespace rewrite while still ensuring integration can validate the coupled behavior. | Accepted / closed through Creator, Checker, and Reviewer. Checker passed `cargo-check`, `make-kernel`, and four exact-name namespace-refresh ktests; Reviewer made no `.rs` edits after artifact-format repair, so final Checker was skipped. |
| `pass_04_directory_metadata_projection_and_update_integration` | Meso Integration Pass | End-to-end Designer Section 3 scenarios after pass01-pass03 acceptance: namespace mutation refresh exposes coherent source / target directory metadata; failure paths preserve the prior durable and in-memory metadata view; root and ordinary directory metadata remain separated; concurrent readers observe either the pre-update or post-update coherent directory metadata view, never torn entry-set bytes versus stale in-memory metadata. Covered micro-features: all meso10 micro-features. | Integration is meaningful only after read projection, direct setters, and namespace-refresh publication all exist. It stays Checker-owned and must not fold new production behavior into the integration lane. | Accepted/closed: Checker passed cargo-check, make-kernel, and three exact-name integration ktests; Reviewer approved the dedicated integration test module, thin ktest wrappers, and narrow test-only write-control disk helper with no `.rs` edits. |
| `pass_05_directory_metadata_projection_coherence_repair_01` | Creator-Synced Repair Pass | Narrow post-acceptance logic repair for `ExfatInode::directory_metadata_projection`: ordinary-directory projection must not snapshot in-memory metadata before the read-side admission boundary that serializes ordinary-directory self-entry republish. Covered micro-features: durable DOS attributes / timestamps, mount-derived owner/mode projection, writable-bit projection, explicit Asterinas metadata answers, and typed refusal distinctions. | The April 29 structural audit and main-agent inspection found a live correctness risk: a projection can combine newer durable self-entry bytes with stale in-memory mode / identity fields if it snapshots `self.metadata` before parent-directory read admission. This repair is narrow enough for a Creator pass using the accepted meso10 Designer contract; it should reuse the existing owner-local no-lock `read_directory_bytes_for_stream(...)` helper and add no new helper unless needed to isolate pure projection assembly from fresh metadata plus validated entry-set bytes. | Creator structurally accepted; user explicitly requested no Checker pass while test code is messy, only cargo-check. Cargo-check passed with receipt `.agents/checker-runs/post_cleanup_repairs_20260429/20260429-094902-post_cleanup_repairs_20260429-problem1_problem3_creator_changes_cargo_check/summary.tsv`. Creator dispatch/artifact: `.agents/subagent-tasks/meso_10_directory_metadata_projection_and_update/pass_05_directory_metadata_projection_coherence_repair_01_creator_dispatch.md`, `.agents/components/meso_10_directory_metadata_projection_and_update/pass_05_directory_metadata_projection_coherence_repair_01_creator.md`. |

**Deferred / Exit Notes:**

- Do not reopen `meso_09_file_metadata_projection_and_update` for directory metadata. Meso10 may reuse already accepted helper shapes only when they fit the directory owner boundary and must still census / justify any surviving helper or carrier in its own write-set.
- Do not redefine lookup identity, alias coherence, slot reservation, rename ordering, or emptiness gates. Those remain owned by `meso_03_directory_lookup_and_identity` and `meso_04_directory_entry_mutation`.
- Do not route directory-local metadata dirtiness into mount-wide clean / dirty publication. File-local durability remains with `meso_07_file_sync_and_persistence`; mount-wide posture and volume flags remain with `meso_08_filesystem_sync_and_volume_state`.
- Structural cleanup is not pre-sliced. If Reviewer rejects helper placement, carrier seams, root synthetic metadata seams, timestamp helper families, or test-support topology, open a named cleanup pass with each objective individually listed.

### `meso_11_volume_admin_identity`

**Decision:** Architect and Designer artifacts are accepted. Implementation is sliced into one broad Creator-synced pass plus one independent meso integration Checker pass. Keep this meso scoped to the `ExfatFs`-owned volume-administrative identity and control boundary: volume label / GUID administrative entries, typed Linux-shaped management ABI carrier or deliberate refusal, forced-shutdown admission, and administrative trim / FITRIM admission / refusal. Do not absorb ordinary namespace lookup or mutation from `meso_03_directory_lookup_and_identity` / `meso_04_directory_entry_mutation`, file or directory metadata from `meso_09_file_metadata_projection_and_update` / `meso_10_directory_metadata_projection_and_update`, free-space scan / Allocation Bitmap truth / discard downgrade semantics from `meso_02_free_space_accounting_and_discard`, or mount-wide forced-shutdown aftermath from `meso_08_filesystem_sync_and_volume_state`.

| Pass ID | Kind | Covered Boundary | Slicing Rationale | Decision State |
| :--- | :--- | :--- | :--- | :--- |
| `pass_01_volume_admin_identity_boundary` | Creator-Synced Pass | The single `ExfatFs` administrative boundary for all meso11 micro-features: `Volume label and volume GUID live as dedicated administrative directory entries`; `FITRIM and online discard are distinct administrative free-space paths`; `Forced shutdown is an administrative state transition with follow-on runtime effects`; `Volume label updates are privileged, encoding-aware, and refusal-prone on lossy conversion`; `Refusal paths must preserve typed errno distinctions instead of collapsing into implicit gaps`; `Administrative Linux ABIs still require an explicit Asterinas carrier or deliberate refusal surface`. The pass must implement or deliberately refuse the Asterinas-local administrative carrier at the `ExfatFs` owner boundary; read / rewrite only dedicated `VolumeIdentityEntries` for label / GUID state; preserve explicit unset identity; enforce privilege, read-only, malformed-input / lossy-conversion, unsupported-carrier, post-shutdown, invalid-layout, and device-I/O typed failures; route forced-shutdown admission without redefining meso08 aftermath; and keep administrative trim / FITRIM admission distinct from meso02 allocator semantics. | The user judged the implementation surface small enough for one Creator shot, and the accepted Designer contract intentionally avoids freezing exact Rust function names or carrier names. Keeping the whole meso11 administrative boundary in one Creator pass reduces artificial helper seams between label/GUID identity, forced shutdown, trim refusal/routing, and typed carrier refusal while still leaving runtime proof and integration as Checker-owned gates. | Creator structurally accepted; synchronized Checker dispatched. |
| `pass_02_volume_admin_identity_integration` | Meso Integration Pass | End-to-end Designer Section 3 scenarios after pass01 acceptance: identity query / update / re-query over `VolumeIdentityEntries`; administrative trim admission / refusal through the same `ExfatFs` boundary; unsupported, permission, read-only, lossy / malformed, device-I/O / invalid-layout, and post-shutdown failures remain typed and rollback-safe; repeated identity query is stable; concurrent label/GUID mutation, forced-shutdown admission, and trim request linearize without hierarchy inversion. Covered micro-features: all meso11 micro-features. | Integration is meaningful only after the one-shot administrative boundary exists. It stays Checker-owned and must not fold new production behavior into the integration lane. | Accepted/closed: Checker passed cargo-check, make-kernel, and four exact-name integration ktests; Reviewer approved the dedicated integration test-support module and narrow sibling helper reuse with no `.rs` edits. |
| `pass_03_volume_admin_identity_interface_cleanup_01` | Creator Cleanup Pass | Post-acceptance interface cleanup for the `fs.rs` / `volume.rs` volume-admin seam after the accepted Designer overlay removed the stale narrow-authority / exact-signature constraint. The pass covers all meso11 micro-features only to preserve behavior while removing compatibility re-exports and one-method proxy surfaces. | The April 29 Designer repair established that `ExfatFs` remains runtime owner, `volume.rs` is the owner-adjacent implementation module, carrier types should be imported from their owning module path, and tests must not force production proxies to survive. This cleanup is a narrow structural pass over the interface shape, not new meso11 functionality. | Creator structurally accepted; user explicitly requested no Checker pass while test code is messy, only cargo-check. Cargo-check passed with receipt `.agents/checker-runs/post_cleanup_repairs_20260429/20260429-094902-post_cleanup_repairs_20260429-problem1_problem3_creator_changes_cargo_check/summary.tsv`. Creator dispatch/artifact: `.agents/subagent-tasks/meso_11_volume_admin_identity/pass_03_volume_admin_identity_interface_cleanup_01_creator_dispatch.md`, `.agents/components/meso_11_volume_admin_identity/pass_03_volume_admin_identity_interface_cleanup_01_creator.md`. |

**Deferred / Exit Notes:**

- If Asterinas lacks a public Linux-shaped carrier for any administrative operation, pass01 must return a deliberate `EOPNOTSUPP`-class refusal or use an owner-local `ExfatFs` adapter. It must not pretend an ordinary inode / path API is the administrative home.
- Administrative trim may preserve the current explicit unsupported boundary until a real lower trim primitive exists. Do not implement allocator range scan, bitmap mutation, or online-discard downgrade here.
- Forced shutdown admission may use the existing mount-wide state transition surface, but post-shutdown read / write / sync / discard consequences remain owned by `meso_08_filesystem_sync_and_volume_state` and `meso_02_free_space_accounting_and_discard`.
- Structural cleanup is not pre-sliced. If Reviewer rejects helper placement, carrier seams, root administrative-entry seams, label encoding helper families, or test-support topology, open a named cleanup pass with each objective individually listed.

## Process Notes From Cleanup Gaps

- `production_code_quality_cleanup_20260428` is a user-directed post-acceptance cleanup wave over already accepted production code. It is intentionally sliced into three serialized Creator cleanup packets instead of reopening Architect / Designer gates: `pass_01_02_production_code_quality_cleanup_creator`, `pass_03_04_production_code_quality_cleanup_creator`, and `pass_05_06_production_code_quality_cleanup_creator`.
- The first packet owns Phase 1 safety / dead-code / dedup / inline cleanup and Phase 2 timestamp plus owner/group body-isomorphism cleanup. The second owns Phase 3 rewrite / NoFatChain cleanup and Phase 4 error / admin-carrier / visibility cleanup. The third owns Phase 5 `inode/` module split and Phase 6 `admin.rs` split.
- This wave uses dispatch stubs and live handoff recording from the protocol, but the user explicitly directed that the full protocol should not constrain code-structure changes needed for the cleanup plan.
- `meso_02` cleanup split happened because the original cleanup packet grouped owner/helper placement and carrier/error-boundary work together; visible progress on Line A was briefly mistaken for full cleanup closure.
- Future cleanup packets must list structural objectives individually, and Creator / Reviewer artifacts must disposition each objective independently.
- Checker run archives now live under `.agents/checker-runs/<meso-component>/...` so evidence remains grouped by parent meso-component.

## Rebase Watch Notes

- `meso_07_file_sync_and_persistence` introduced `PageCache::has_dirty_pages()` / `PageCacheManager::has_dirty_pages()` in `kernel/src/fs/vfs/page_cache.rs` as a narrow VFS page-cache dirty-range query. This was not a broad VFS redesign: it closed the pass02 repeated-clean `sync_data()` / `sync_all()` bug where a materialized but clean page cache was mistaken for pending writeback and caused flush-only device sync BIOs. During future upstream rebases, check whether upstream `PageCache` grew an equivalent dirty-range predicate, dirty-tag query, or `evict_range()` writeback-result API. If so, prefer converging to that upstream API and remove the local query. If not, keep this interface visible in review because exFAT refactor's file-sync clean-call stability depends on distinguishing "page cache exists" from "range contains dirty cached pages".
